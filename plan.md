# 🚀 План внедрения RAG + KAG Lite

## 📋 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Пользовательский UI                      │
│  • Умный поиск транзакций                                   │
│  • AI чат-ассистент                                         │
│  • Рекомендации и инсайты                                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      RAG Layer                               │
│  • Векторный поиск (pgvector)                               │
│  • Embeddings генерация (OpenAI)                            │
│  • Контекст для LLM                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    KAG Lite Layer                            │
│  • JSON граф отношений                                      │
│  • Анализ паттернов                                         │
│  • Рекомендательный движок                                  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  Supabase + OpenRouter                      │
│  • PostgreSQL + pgvector                                    │
│  • OpenRouter: openai/gpt-4o-mini для генерации             │
│  • OpenRouter: openai/text-embedding-3-small для embeddings │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 Этап 1: Подготовка БД (Embeddings) - День 1

### ✅ Задачи:
1. [x] Добавить колонку `embedding` в таблицу `transactions`
2. [x] Создать индекс HNSW для векторного поиска
3. [x] Создать SQL функции для поиска
4. [ ] Добавить триггер для автоматической генерации embeddings (отложено - делаем через API)

### 📝 Файлы:
- `db/migrations/20250102_add_embeddings.sql`

### 🔧 Реализация:
```sql
-- 1. Добавить колонку embedding
ALTER TABLE transactions ADD COLUMN embedding vector(1536);

-- 2. Создать индекс
CREATE INDEX ON transactions USING hnsw (embedding vector_cosine_ops);

-- 3. Функция поиска
CREATE FUNCTION match_transactions(
  query_embedding vector(1536),
  match_threshold float,
  match_count int
)...
```

---

## 🎯 Этап 2: Генерация Embeddings - День 1-2

### ✅ Задачи:
1. [x] Создать API endpoint для генерации embeddings
2. [ ] Создать фоновую задачу для существующих транзакций  
3. [ ] Добавить автогенерацию при создании транзакции
4. [x] Создать утилиту для текстового представления транзакции

### 📝 Файлы:
- `app/api/embeddings/generate/route.ts` - API генерации
- `lib/ai/embeddings.ts` - Утилиты embeddings
- `lib/ai/transaction-text.ts` - Текстовое представление

### 🔧 Реализация:
```typescript
// Генерация embedding для транзакции
async function generateTransactionEmbedding(transaction) {
  const text = transactionToText(transaction);
  const embedding = await openai.embeddings.create({
    model: "text-embedding-3-small",
    input: text
  });
  return embedding.data[0].embedding;
}
```

---

## 🎯 Этап 3: RAG - Векторный поиск - День 2-3

### ✅ Задачи:
1. [ ] Создать компонент умного поиска транзакций
2. [ ] Реализовать API для векторного поиска
3. [ ] Добавить UI для поиска на странице транзакций
4. [ ] Добавить фильтры и сортировку результатов

### 📝 Файлы:
- `app/api/search/transactions/route.ts` - API поиска
- `components/search/SmartSearch.tsx` - UI компонент
- `lib/ai/vector-search.ts` - Логика поиска

### 🔧 Реализация:
```typescript
// Умный поиск
async function smartSearch(query: string) {
  // 1. Генерируем embedding запроса
  const queryEmbedding = await generateEmbedding(query);
  
  // 2. Ищем похожие транзакции
  const matches = await supabase.rpc('match_transactions', {
    query_embedding: queryEmbedding,
    match_threshold: 0.7,
    match_count: 20
  });
  
  return matches;
}
```

---

## 🎯 Этап 4: RAG - AI Чат-ассистент - День 3-4

### ✅ Задачи:
1. [x] Создать компонент чата (УЖЕ ЕСТЬ: `/ai-chat`)
2. [ ] Интегрировать RAG pipeline в существующий чат
3. [ ] Добавить контекст из транзакций в ответы
4. [x] Системный промпт для финансового ассистента (УЖЕ ЕСТЬ)
5. [ ] Добавить умный поиск транзакций в чат

### 📝 Файлы:
- `app/(protected)/ai-chat/Chat.tsx` - СУЩЕСТВУЮЩИЙ UI чата
- `app/(protected)/ai-chat/actions.ts` - СУЩЕСТВУЮЩИЕ действия
- `lib/ai/rag-pipeline.ts` - RAG логика (СОЗДАТЬ)
- `lib/ai/chat-context.ts` - Контекст для чата (СОЗДАТЬ)

### 🔧 Реализация:
```typescript
// RAG Pipeline
async function answerQuestion(question: string, userId: string) {
  // 1. Векторный поиск релевантных транзакций
  const context = await searchRelevantTransactions(question, userId);
  
  // 2. Получение дополнительных данных
  const accounts = await getAccounts(userId);
  const budgets = await getBudgets(userId);
  
  // 3. Формирование промпта
  const prompt = buildPrompt(question, context, accounts, budgets);
  
  // 4. Запрос к LLM
  const response = await openai.chat.completions.create({
    model: "gpt-4o-mini",
    messages: [
      { role: "system", content: FINANCIAL_ASSISTANT_PROMPT },
      { role: "user", content: prompt }
    ]
  });
  
  return response.choices[0].message.content;
}
```

---

## 🎯 Этап 5: KAG Lite - Граф отношений - День 4-5

### ✅ Задачи:
1. [ ] Создать таблицу для хранения графа отношений
2. [ ] Реализовать построение графа из транзакций
3. [ ] Создать API для анализа графа
4. [ ] Добавить обнаружение паттернов

### 📝 Файлы:
- `db/migrations/20250103_knowledge_graph.sql`
- `app/api/ai/graph/build/route.ts` - Построение графа
- `lib/ai/knowledge-graph.ts` - Работа с графом
- `lib/ai/pattern-detection.ts` - Обнаружение паттернов

### 🔧 Реализация:
```sql
-- Таблица графа отношений
CREATE TABLE knowledge_graph (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users NOT NULL,
  entity_type text NOT NULL, -- 'transaction', 'category', 'account', 'merchant'
  entity_id uuid NOT NULL,
  related_type text NOT NULL,
  related_id uuid NOT NULL,
  relation_type text NOT NULL, -- 'belongs_to', 'follows', 'similar_to'
  strength float NOT NULL, -- 0.0 - 1.0
  metadata jsonb,
  created_at timestamptz DEFAULT now()
);
```

```typescript
// Построение графа
async function buildKnowledgeGraph(userId: string) {
  const transactions = await getTransactions(userId);
  
  for (const txn of transactions) {
    // 1. Связь: транзакция → категория
    await addRelation({
      entity: { type: 'transaction', id: txn.id },
      related: { type: 'category', id: txn.category_id },
      relation: 'belongs_to',
      strength: 1.0
    });
    
    // 2. Связь: транзакция → счет
    await addRelation({
      entity: { type: 'transaction', id: txn.id },
      related: { type: 'account', id: txn.account_id },
      relation: 'from_account',
      strength: 1.0
    });
    
    // 3. Связь: последовательность транзакций
    const prevTxn = findPreviousTransaction(txn);
    if (prevTxn) {
      await addRelation({
        entity: { type: 'transaction', id: prevTxn.id },
        related: { type: 'transaction', id: txn.id },
        relation: 'followed_by',
        strength: 0.8
      });
    }
  }
}
```

---

## 🎯 Этап 6: KAG Lite - Анализ паттернов - День 5-6

### ✅ Задачи:
1. [ ] Реализовать обнаружение временных паттернов
2. [ ] Обнаружение связанных покупок
3. [ ] Анализ трат по дням недели/времени
4. [ ] Создание сводки паттернов

### 📝 Файлы:
- `lib/ai/patterns/temporal.ts` - Временные паттерны
- `lib/ai/patterns/sequential.ts` - Последовательные паттерны
- `lib/ai/patterns/clustering.ts` - Кластеризация

### 🔧 Реализация:
```typescript
// Обнаружение паттернов
async function detectPatterns(userId: string) {
  const graph = await getKnowledgeGraph(userId);
  
  // 1. Временные паттерны
  const temporalPatterns = analyzeTemporalPatterns(graph);
  // Результат: "Рестораны → Пятница вечер (85%)"
  
  // 2. Последовательные паттерны
  const sequentialPatterns = analyzeSequentialPatterns(graph);
  // Результат: "Ресторан → Такси (82% случаев)"
  
  // 3. Кластеры трат
  const spendingClusters = clusterTransactions(graph);
  // Результат: "Выходные: 3000₽ средний чек ресторанов"
  
  return {
    temporal: temporalPatterns,
    sequential: sequentialPatterns,
    clusters: spendingClusters
  };
}
```

---

## 🎯 Этап 7: Рекомендательная система - День 6-7

### ✅ Задачи:
1. [ ] Создать движок рекомендаций
2. [ ] Реализовать типы рекомендаций
3. [x] UI для рекомендаций (УЖЕ ЕСТЬ: `/ai-advisor`)
4. [x] UI для аналитики (УЖЕ ЕСТЬ: `/ai-analytics`)
5. [ ] Интегрировать RAG+KAG в существующие страницы

### 📝 Файлы:
- `app/api/ai/recommendations/route.ts` - API рекомендаций (СОЗДАТЬ)
- `app/(protected)/ai-advisor/AIAdvisorClient.tsx` - СУЩЕСТВУЮЩИЙ UI
- `app/(protected)/ai-analytics/` - СУЩЕСТВУЮЩАЯ страница
- `lib/ai/recommender.ts` - Движок рекомендаций (СОЗДАТЬ)

### 🔧 Реализация:
```typescript
// Типы рекомендаций
enum RecommendationType {
  BUDGET_WARNING = 'budget_warning',
  SAVINGS_OPPORTUNITY = 'savings_opportunity',
  SPENDING_PATTERN = 'spending_pattern',
  OPTIMIZATION = 'optimization'
}

// Генерация рекомендаций
async function generateRecommendations(userId: string) {
  const patterns = await detectPatterns(userId);
  const budgets = await getBudgets(userId);
  const recommendations = [];
  
  // 1. Предупреждения о бюджете
  for (const budget of budgets) {
    if (budget.used / budget.limit > 0.9) {
      recommendations.push({
        type: RecommendationType.BUDGET_WARNING,
        title: `Превышение бюджета "${budget.category}"`,
        description: `Использовано ${budget.used}₽ из ${budget.limit}₽`,
        action: "Сократить траты на категорию"
      });
    }
  }
  
  // 2. Возможности экономии
  if (patterns.sequential['restaurant_taxi'] > 0.8) {
    recommendations.push({
      type: RecommendationType.SAVINGS_OPPORTUNITY,
      title: "Экономия на такси",
      description: "Вы часто берете такси после ресторана",
      action: "Рассмотрите подписку Яндекс.Плюс (экономия 30%)"
    });
  }
  
  return recommendations;
}
```

---

## 🎯 Этап 8: UI/UX интеграция - День 7-8

### ✅ Задачи:
1. [ ] Добавить кнопку AI помощника в навигацию
2. [ ] Создать страницу AI Dashboard
3. [ ] Добавить виджет рекомендаций на главную
4. [ ] Создать страницу аналитики паттернов
5. [ ] Добавить онбординг для AI функций

### 📝 Файлы:
- `app/(protected)/ai/page.tsx` - AI Dashboard
- `components/ai/AIDashboard.tsx` - Основной компонент
- `components/ai/PatternInsights.tsx` - Инсайты паттернов
- `components/ai/AIOnboarding.tsx` - Онбординг

---

## 📊 Метрики успеха

### KPI:
- [ ] **Точность поиска**: >85% релевантных результатов
- [ ] **Качество ответов**: >4/5 оценка пользователей
- [ ] **Полезность рекомендаций**: >70% применяют
- [ ] **Производительность**: <2s на запрос
- [ ] **Стоимость**: <$30/месяц на OpenAI API

---

## 💰 Оценка стоимости

### OpenAI API (на 1000 пользователей):
- **Embeddings**: ~10,000 транзакций/день × $0.00002 = $0.20/день
- **Chat completions**: ~500 запросов/день × $0.0001 = $0.05/день
- **Итого**: ~$7.50/месяц

### Инфраструктура:
- **Supabase**: включено в план
- **pgvector**: бесплатно (расширение Postgres)

### Общая стоимость: $10-15/месяц

---

## 🚀 Порядок разработки

### День 1: Фундамент
1. Миграция БД (embeddings)
2. Базовая генерация embeddings
3. Функции векторного поиска

### День 2-3: RAG Core
1. API векторного поиска
2. UI умного поиска
3. RAG pipeline

### День 3-4: AI Ассистент
1. Компонент чата
2. API чата с RAG
3. Системные промпты

### День 4-5: KAG Lite
1. Таблица графа
2. Построение графа
3. Анализ паттернов

### День 6-7: Рекомендации
1. Движок рекомендаций
2. UI компоненты
3. Интеграция в дашборд

### День 8: Полировка
1. Тестирование
2. Оптимизация
3. Документация

---

## ⚠️ Риски и митигация

### Риск 1: Высокая стоимость API
**Митигация**: Кэширование результатов, батчинг запросов

### Риск 2: Медленная генерация embeddings
**Митигация**: Фоновые задачи, очередь

### Риск 3: Низкое качество рекомендаций
**Митигация**: A/B тестирование, обратная связь

---

## 📚 Следующие шаги

После завершения базовой реализации:
1. [ ] Добавить голосовой ввод для чата
2. [ ] Реализовать экспорт инсайтов в PDF
3. [ ] Добавить прогнозирование трат
4. [ ] Интеграция с внешними сервисами (банки)
5. [ ] Мобильное приложение с AI

---

## 🎉 Текущий статус

**Начало разработки**: 02.11.2025, 23:30

### В процессе:
- [ ] Этап 3: RAG - Векторный поиск (75% выполнено - API готов, нужен UI)
- [ ] Этап 4: Интеграция RAG в существующий AI чат

### Завершено:
- [x] Планирование и архитектура
- [x] **Этап 1**: Подготовка БД (миграция применена через Supabase MCP)
  - [x] Расширение pgvector включено
  - [x] Колонка embedding добавлена
  - [x] Индексы HNSW созданы
  - [x] Функции match_transactions() и get_embedding_stats()
- [x] **Этап 2**: Генерация Embeddings (75%)
  - [x] API `/api/ai/embeddings/generate` (POST/GET)
  - [x] Утилиты buildTransactionText() и createEmbedding()
  - [x] Использование OpenRouter вместо OpenAI
- [x] **Этап 3**: RAG Векторный поиск (75%)
  - [x] API `/api/ai/search/transactions` для умного поиска
  - [x] Обогащение результатов категориями и счетами

### Существующие AI страницы (готовы к интеграции):
- ✅ `/ai-chat` - AI чат-ассистент (OpenRouter)
- ✅ `/ai-advisor` - AI финансовый советник
- ✅ `/ai-analytics` - AI аналитика

### Следующие шаги:
1. Создать RAG pipeline для чата
2. Интегрировать векторный поиск в AI чат
3. Добавить контекст транзакций в ответы чата
4. Создать движок рекомендаций для AI Advisor
5. Интегрировать KAG паттерны в AI Analytics

---

**Последнее обновление**: 02.11.2025, 23:45
