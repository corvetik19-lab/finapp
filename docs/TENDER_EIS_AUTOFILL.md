# Автозаполнение тендеров из ЕИС

## 📋 Описание

Функционал автоматического заполнения формы тендера данными из Единой информационной системы (ЕИС) по номеру закупки.

## 🎯 Как это работает

### 1. **Пользовательский сценарий**

1. Пользователь нажимает кнопку **"➕ Добавить тендер"**
2. Открывается модальное окно поиска с полем ввода номера закупки
3. Пользователь вводит номер (например: `32515383401`)
4. Нажимает кнопку **"🔍 Найти"**
5. Система ищет тендер в ЕИС (эмуляция)
6. При успешном поиске автоматически открывается форма создания тендера с заполненными полями:
   - Номер закупки
   - Предмет закупки
   - Заказчик
   - НМЦК
   - Способ определения
   - Тип (ФЗ-223, ФЗ-44 и т.д.)
   - Площадка
   - Дата окончания подачи заявки
   - Дата подведения итогов
   - Обеспечение заявки/контракта

### 2. **Альтернативный сценарий**

Если тендер не найден или пользователь хочет ввести данные вручную:
- Нажимает кнопку **"✏️ Добавить вручную"**
- Открывается пустая форма создания тендера

## 🏗️ Архитектура

### Компоненты

#### 1. **TenderSearchEISModal** (`components/tenders/tender-search-eis-modal.tsx`)
Модальное окно для поиска тендера по номеру:
- Поле ввода номера закупки
- Кнопка поиска с индикацией загрузки
- Обработка ошибок (тендер не найден)
- Кнопка "Добавить вручную"

#### 2. **TenderFormModal** (обновлен)
Форма создания/редактирования тендера:
- Принимает проп `eisData` для автозаполнения
- Автоматически заполняет поля при наличии данных из ЕИС
- Поддерживает ручное редактирование всех полей

#### 3. **TendersListClient** (обновлен)
Главный компонент списка тендеров:
- Управляет состоянием модалок
- Координирует поток данных между компонентами

### API

#### Endpoint: `/api/tenders/search-eis`

**GET запрос:**
```
/api/tenders/search-eis?purchase_number=32515383401&include_documents=true
```

**Параметры:**
- `purchase_number` (обязательный) - номер закупки
- `include_documents` (опциональный) - включить список документов

**Ответ (успех):**
```json
{
  "success": true,
  "data": {
    "purchase_number": "32515383401",
    "subject": "ОКПД 2 28.23.2 Поставка расходных материалов...",
    "customer": "ПУБЛИЧНОЕ АКЦИОНЕРНОЕ ОБЩЕСТВО...",
    "nmck": 5645255.27,
    "platform": "РусГидро",
    "procurement_method": "Запрос предложений в электронной форме",
    "tender_type": "ФЗ-223",
    "submission_deadline": "2025-11-20T07:00:00Z",
    "results_date": "2025-11-28T00:00:00Z",
    "eis_url": "https://zakupki.gov.ru/..."
  },
  "documents": [
    {
      "id": "1",
      "name": "Техническое задание.pdf",
      "type": "technical_specification",
      "size": 2456789,
      "url": "/mock/documents/tz_32515383401.pdf"
    }
  ]
}
```

**Ответ (не найден):**
```json
{
  "error": "Тендер не найден в ЕИС",
  "purchase_number": "32515383401"
}
```

### Мок-данные

#### Файл: `lib/tenders/eis-mock-data.ts`

Содержит эмуляцию данных ЕИС для тестирования:

**Доступные тестовые номера:**
- `32515383401` - Поставка расходных материалов для оргтехники (ФЗ-223)
- `32312315116` - СИП (самонесущий изолированный провод) (ФЗ-44)
- `32409876543` - Поставка медицинского оборудования (ФЗ-44)

**Функции:**
- `searchTenderInEIS(purchaseNumber)` - поиск тендера по номеру
- `getTenderDocuments(purchaseNumber)` - получение списка документов
- `formatFileSize(bytes)` - форматирование размера файла

## 🎨 UI/UX

### Модалка поиска

**Особенности:**
- Автофокус на поле ввода
- Поиск по Enter
- Индикация загрузки (🔄 Поиск...)
- Отображение ошибок с иконкой ⚠️
- Подсказки для пользователя (💡)
- Адаптивный дизайн (мобильные устройства)

**Анимации:**
- Плавное появление (fadeIn + slideUp)
- Hover-эффекты на кнопках
- Smooth transitions

### Форма тендера

**Автозаполнение:**
- Все поля заполняются автоматически
- Пользователь может редактировать любое поле
- Подсветка заполненных полей
- Валидация по схеме Zod

## 🔧 Интеграция с реальным API ЕИС

### Шаг 1: Получение доступа к API

Зарегистрируйтесь на портале ЕИС и получите API ключ:
- https://zakupki.gov.ru/

### Шаг 2: Обновление endpoint

Замените мок-функцию в `lib/tenders/eis-mock-data.ts`:

```typescript
export async function searchTenderInEIS(
  purchaseNumber: string
): Promise<EISTenderData | null> {
  try {
    const response = await fetch(
      `https://zakupki-api.gov.ru/api/v1/tenders/${purchaseNumber}`,
      {
        headers: {
          'Authorization': `Bearer ${process.env.EIS_API_KEY}`,
          'Content-Type': 'application/json',
        },
      }
    );

    if (!response.ok) {
      return null;
    }

    const data = await response.json();
    
    // Маппинг данных из API ЕИС в наш формат
    return {
      purchase_number: data.regNumber,
      subject: data.purchaseObjectInfo,
      customer: data.customer.fullName,
      nmck: data.maxPrice, // в рублях
      platform: data.placingWay?.name,
      procurement_method: data.procedureInfo?.name,
      tender_type: data.fz,
      submission_deadline: data.applicationEndDate,
      results_date: data.summingupDate,
      eis_url: data.url,
    };
  } catch (error) {
    console.error('Error fetching from EIS:', error);
    return null;
  }
}
```

### Шаг 3: Добавление переменных окружения

```env
# .env.local
EIS_API_KEY=your_api_key_here
EIS_API_URL=https://zakupki-api.gov.ru/api/v1
```

### Шаг 4: Обновление серверного endpoint

```typescript
// app/api/tenders/search-eis/route.ts
import { searchTenderInEIS } from '@/lib/tenders/eis-api'; // реальный API

export async function GET(request: NextRequest) {
  // ... существующий код
}
```

## 📊 Метрики и мониторинг

### Рекомендуемые метрики:

1. **Успешность поиска**
   - Процент найденных тендеров
   - Время ответа API

2. **Использование функционала**
   - Количество поисков в день
   - Соотношение автозаполнения vs ручного ввода

3. **Ошибки**
   - Частота ошибок API
   - Типы ошибок (не найден, timeout, и т.д.)

### Логирование:

```typescript
// Добавьте в searchTenderInEIS
console.log('[EIS] Searching tender:', purchaseNumber);
console.log('[EIS] Found:', !!tenderData);
console.log('[EIS] Response time:', responseTime, 'ms');
```

## 🧪 Тестирование

### Unit тесты

```typescript
// __tests__/lib/tenders/eis-mock-data.test.ts
import { searchTenderInEIS } from '@/lib/tenders/eis-mock-data';

describe('searchTenderInEIS', () => {
  it('should find tender by number', async () => {
    const result = await searchTenderInEIS('32515383401');
    expect(result).not.toBeNull();
    expect(result?.purchase_number).toBe('32515383401');
  });

  it('should return null for non-existent tender', async () => {
    const result = await searchTenderInEIS('99999999999');
    expect(result).toBeNull();
  });
});
```

### E2E тесты (Playwright)

```typescript
// tests/e2e/tender-eis-autofill.spec.ts
test('should autofill tender form from EIS', async ({ page }) => {
  await page.goto('/tenders/list');
  await page.click('button:has-text("Добавить тендер")');
  
  await page.fill('input[placeholder*="номер тендера"]', '32515383401');
  await page.click('button:has-text("Найти")');
  
  await page.waitForSelector('input[name="purchase_number"]');
  
  const purchaseNumber = await page.inputValue('input[name="purchase_number"]');
  expect(purchaseNumber).toBe('32515383401');
  
  const subject = await page.inputValue('input[name="subject"]');
  expect(subject).toContain('расходных материалов');
});
```

## 🚀 Будущие улучшения

1. **Кэширование результатов поиска**
   - Redis для хранения часто запрашиваемых тендеров
   - TTL: 24 часа

2. **Автоматическая загрузка документов**
   - Скачивание ТЗ, проекта контракта
   - Сохранение в Supabase Storage

3. **История поиска**
   - Сохранение последних поисков пользователя
   - Быстрый доступ к ранее найденным тендерам

4. **Умные подсказки**
   - Автодополнение номера закупки
   - Проверка формата номера

5. **Batch импорт**
   - Загрузка нескольких тендеров из CSV
   - Массовый поиск в ЕИС

## 📝 Примечания

- Текущая реализация использует мок-данные для демонстрации
- Для продакшена необходима интеграция с реальным API ЕИС
- Все суммы хранятся в рублях (number с плавающей точкой)
- Даты в формате ISO 8601 (UTC)

## 🤝 Поддержка

При возникновении вопросов или проблем:
1. Проверьте логи в консоли браузера
2. Проверьте сетевые запросы в DevTools
3. Убедитесь, что номер закупки корректен
4. Проверьте доступность API ЕИС
