# Руководство по шаблонам этапов тендеров

## Описание

Система шаблонов этапов позволяет создавать наборы этапов (например "ФЗ-44", "ЗМО", "Коммерческие") и быстро применять их к тендерам.

## Возможности

### Создание шаблона

1. Перейдите в **Настройки тендеров** → вкладка **Шаблоны этапов** 📚
2. Нажмите **Создать шаблон**
3. Укажите:
   - **Название** (например: "ФЗ-44", "ЗМО", "Коммерческие")
   - **Описание** (опционально)
   - **Иконка** (эмодзи для визуального отличия)
   - **Выберите этапы** из доступных (слева)
4. Упорядочьте выбранные этапы (справа) с помощью стрелок ⬆️⬇️
5. Нажмите **Сохранить**

### Редактирование шаблона

1. В списке шаблонов найдите нужный
2. Нажмите ✏️ (Редактировать)
3. Измените название, описание, этапы или их порядок
4. Сохраните изменения

### Удаление шаблона

1. Нажмите 🗑️ (Удалить) на карточке шаблона
2. Подтвердите действие

## Примеры шаблонов

### ФЗ-44 (Федеральный закон о госзакупках)
```
Иконка: 📜
Этапы:
1. Анализ и просчёт
2. Проверка
3. Подан. Рассмотрение заявки
4. Торги
5. Ждём итоги
6. Победа. Ждём договор
7. Заключение договора
8. Договор подписан
```

### ЗМО (Закупки малого объема)
```
Иконка: 📝
Этапы:
1. Анализ и просчёт
2. Подан. Рассмотрение заявки
3. Победа. Ждём договор
4. Договор подписан
```

### Коммерческие тендеры
```
Иконка: 💼
Этапы:
1. Анализ и просчёт
2. Проверка
3. Торги
4. Заключение договора
```

## Структура БД

### Таблицы

**tender_stage_templates** - шаблоны наборов этапов
- `id` - UUID
- `company_id` - ID компании
- `name` - название шаблона
- `description` - описание
- `icon` - иконка (эмодзи)
- `is_active` - активность
- `created_at`, `updated_at` - временные метки

**tender_stage_template_items** - этапы в составе шаблона
- `id` - UUID
- `template_id` - ID шаблона
- `stage_id` - ID этапа
- `order_index` - порядковый номер

### RLS политики

Все операции доступны только членам компании (проверка через `company_members`).

## API

### GET /api/tenders/stage-templates
Получить все шаблоны компании с вложенными этапами

### POST /api/tenders/stage-templates
Создать новый шаблон
```json
{
  "name": "ФЗ-44",
  "description": "Этапы для тендеров по 44-ФЗ",
  "icon": "📜",
  "stage_ids": ["uuid1", "uuid2", "uuid3"]
}
```

### PATCH /api/tenders/stage-templates/:id
Обновить шаблон

### DELETE /api/tenders/stage-templates/:id
Удалить шаблон

## Преимущества

✅ **Быстрое создание** - применяйте готовый набор этапов одним кликом
✅ **Консистентность** - все тендеры одного типа используют одинаковые этапы
✅ **Гибкость** - создавайте неограниченное количество шаблонов
✅ **Удобство** - визуальные иконки для быстрого поиска
✅ **Порядок** - задавайте последовательность этапов в шаблоне

## TODO: Применение шаблона при создании тендера

В следующем обновлении будет добавлена возможность выбора шаблона при создании тендера. Это автоматически установит соответствующие этапы для нового тендера.
