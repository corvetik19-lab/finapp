# 🚀 Быстрая настройка логистики

## Шаг 1: Применить миграцию в Supabase

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Скопируйте содержимое файла `db/migrations/0103_create_logistics_tables.sql`
5. Вставьте в SQL Editor
6. Нажмите **Run** (или Ctrl+Enter)

## Шаг 2: Проверьте создание таблиц

В SQL Editor выполните:
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('shipments', 'drivers', 'shipment_status_history');
```

Должно вернуть 3 таблицы.

## Шаг 3: Откройте страницу логистики

Перейдите по адресу:
```
http://localhost:3000/logistics
```

**НЕ** `/tenders/logistics` - это неправильный URL!

## Шаг 4: Создайте первую отправку

1. Нажмите кнопку **"Новая отправка"**
2. Заполните форму
3. Нажмите **"Создать отправку"**

## Возможные проблемы

### Ошибка: "relation does not exist"
**Решение:** Миграция не применена. Вернитесь к Шагу 1.

### Ошибка: "permission denied"
**Решение:** RLS политики не созданы. Убедитесь, что вся миграция выполнена полностью.

### Страница пустая
**Решение:** 
1. Проверьте консоль браузера (F12)
2. Проверьте, что вы авторизованы
3. Проверьте URL - должен быть `/logistics`

## Быстрый тест

Выполните в SQL Editor:
```sql
-- Проверка таблиц
SELECT COUNT(*) as shipments_count FROM shipments;
SELECT COUNT(*) as drivers_count FROM drivers;

-- Проверка RLS
SELECT tablename, policyname 
FROM pg_policies 
WHERE tablename IN ('shipments', 'drivers');
```

---

**После применения миграции страница заработает!** 🎉
