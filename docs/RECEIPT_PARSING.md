# Парсинг чеков с помощью AI

## Как работает функция "Добавить чек"

### Обзор
Функция автоматически распознаёт текст кассового чека и создаёт транзакцию с позициями товаров, используя OpenAI GPT-4o-mini для парсинга и сопоставления товаров.

### Процесс работы

#### 1. Ввод текста чека
- Пользователь вставляет текст чека в модальное окно
- Текст отправляется на сервер для обработки в режиме предпросмотра

#### 2. Парсинг чека (OpenAI)
API отправляет текст в OpenAI с промптом для извлечения:
- **Название магазина** (`storeName`)
- **Дата покупки** (`date`) в формате YYYY-MM-DD
- **Список товаров** (`items`):
  - Название товара
  - Количество
  - Цена за единицу
  - Общая сумма
- **Итоговая сумма** (`totalAmount`)

**Пример ответа от OpenAI:**
```json
{
  "storeName": "Пятёрочка",
  "date": "2025-11-10",
  "items": [
    {
      "name": "Онигири Фуджи с Креветкой 120г",
      "quantity": 2,
      "pricePerUnit": 89.90,
      "total": 179.80
    },
    {
      "name": "Батончик Корнлайн кокос 30г",
      "quantity": 1,
      "pricePerUnit": 45.00,
      "total": 45.00
    }
  ],
  "totalAmount": 224.80
}
```

#### 3. Предпросмотр и редактирование
После парсинга пользователь видит:
- **Название магазина** (можно редактировать)
- **Дата** (можно изменить через date picker)
- **Список товаров** с ценами (только просмотр)
- **Итоговую сумму**

Пользователь может:
- ✏️ Отредактировать название магазина
- 📅 Изменить дату покупки
- ✅ Сохранить чек
- ❌ Отменить и начать заново

#### 4. Сопоставление товаров с БД

При сохранении система ищет соответствия товаров из чека с товарами в базе данных:

**a) Точное совпадение**
```
"Онигири" из чека → "Онигири" в БД ✅
```

**b) Частичное совпадение**
```
"Онигири Фуджи с Креветкой 120г" → "Онигири" ✅
"Батончик Корнлайн кокос 30г" → "Батончик" ✅
```

**c) AI-сопоставление (для сложных случаев)**
Если автоматическое сопоставление не сработало, система использует второй запрос к OpenAI:
- Отправляет список не найденных товаров из чека
- Отправляет список всех товаров в БД
- AI находит смысловые соответствия с уверенностью > 70%

**Пример AI-сопоставления:**
```
"Coca-Cola Zero 0.5л" → "Кола" (уверенность: 95%)
"Молоко Простоквашино 3.2%" → "Молоко" (уверенность: 90%)
```

#### 5. Создание транзакции

После сопоставления система:
1. Находит счёт пользователя (первый доступный или указанный)
2. Создаёт транзакцию типа "расход":
   - Сумма: `-totalAmount` (в копейках)
   - Контрагент: название магазина
   - Дата: дата из чека
   - Примечание: "Покупка в {storeName}"
3. Добавляет позиции товаров:
   - Использует **короткое название** из БД (не из чека)
   - Сохраняет количество и цены
   - Привязывает категорию товара (если есть)
4. Обновляет баланс счёта

#### 6. Результат

Пользователь получает сообщение с итогами:
```
✅ Чек обработан!

🏪 Магазин: Пятёрочка
📅 Дата: 2025-11-10
💰 Сумма: 224.80 ₽
📦 Товаров: 2

✅ Добавлено позиций: 2
✅ Онигири Фуджи с Креветкой 120г → Онигири (Питание)
✅ Батончик Корнлайн кокос 30г → Батончик (Прочее)
```

Если товары не найдены:
```
⚠️ Товары не найдены в БД:
Жевательная резинка Ментос мята свежая

Создайте эти товары в системе для автоматического распознавания.
```

## API Endpoints

### POST /api/ai/process-receipt

**Параметры:**
- `receiptText` (string, required) - полный текст чека
- `accountName` (string, optional) - название счёта для списания
- `preview` (boolean, optional) - режим предпросмотра (true = не сохранять)

**Ответ (preview mode):**
```json
{
  "success": true,
  "preview": true,
  "data": {
    "storeName": "Пятёрочка",
    "date": "2025-11-10",
    "items": [...],
    "totalAmount": 224.80
  },
  "message": "Предпросмотр чека готов"
}
```

**Ответ (save mode):**
```json
{
  "success": true,
  "message": "✅ Чек обработан!...",
  "data": {
    "transactionId": "uuid",
    "addedItems": 2,
    "notFoundItems": 0
  }
}
```

## Требования

### Для корректной работы необходимо:

1. **OpenAI API Key** в переменных окружения:
   ```
   OPENAI_API_KEY=sk-...
   ```

2. **Активные товары в БД** (`product_items`):
   - `is_active = true`
   - Привязаны к `user_id`
   - Имеют категории (опционально)

3. **Хотя бы один счёт** в таблице `accounts`

## Ограничения

- Работает только с текстовыми чеками (не изображения)
- Требует наличия товаров в БД для сопоставления
- Дата чека не должна быть старше 1 года
- Максимальная точность сопоставления ~95% (зависит от качества чека)

## Примеры использования

### Пример 1: Простой чек
```
Пятёрочка
10.11.2025

Онигири 2шт x 89.90 = 179.80
Батончик 1шт x 45.00 = 45.00

ИТОГО: 224.80
```

### Пример 2: Чек с длинными названиями
```
МАГНИТ
Дата: 10.11.2025

1. Онигири Фуджи с Креветкой 120г
   2 x 89.90 = 179.80

2. Батончик Корнлайн кокос 30г
   1 x 45.00 = 45.00

Итого к оплате: 224.80 руб.
```

Оба чека будут корректно распознаны и сопоставлены с товарами в БД.

## Troubleshooting

### Товары не найдены
**Проблема:** Система не может найти товары из чека в БД

**Решение:**
1. Откройте "Настройки" → "Товары"
2. Добавьте товары с **короткими названиями**:
   - ✅ "Онигири" (вместо "Онигири Фуджи с Креветкой 120г")
   - ✅ "Батончик" (вместо "Батончик Корнлайн кокос 30г")
3. Убедитесь что `is_active = true`

### Неправильная дата
**Проблема:** Дата распознана некорректно

**Решение:**
- Отредактируйте дату в предпросмотре перед сохранением
- Система автоматически валидирует дату (не старше 1 года)

### Ошибка "Счёт не найден"
**Проблема:** У пользователя нет активных счетов

**Решение:**
1. Создайте хотя бы один счёт в разделе "Карты"
2. Убедитесь что `deleted_at IS NULL`

## Технические детали

### Используемые технологии
- **OpenAI GPT-4o-mini** - парсинг и AI-сопоставление
- **Supabase** - хранение данных
- **Next.js API Routes** - серверная обработка
- **React** - UI компонент

### Файлы
- `components/receipts/ReceiptChatModal.tsx` - UI компонент
- `app/api/ai/process-receipt/route.ts` - API endpoint
- `lib/ai/tool-handlers.ts` - логика обработки (функция `processReceipt`)
- `lib/ai/tools.ts` - схемы и описания инструментов

### База данных
**Таблицы:**
- `transactions` - транзакции
- `transaction_items` - позиции товаров
- `product_items` - справочник товаров
- `accounts` - счета пользователя
- `categories` - категории
