# 📊 Анализ CRM и архитектура улучшенной системы

> **Дата:** 8 ноября 2025  
> **Версия:** 1.0  
> **Статус:** ✅ Готово к разработке

---

## 📋 Содержание

1. [Executive Summary](#executive-summary)
2. [Анализ недостатков конкурента](#анализ-недостатков-конкурента)
3. [AI/RAG/KAG Архитектура](#airagkag-архитектура)
4. [Техническая спецификация](#техническая-спецификация)
5. [UI/UX Концепция](#uiux-концепция)
6. [План разработки](#план-разработки)

---

# Executive Summary

## Цель проекта

Создать современную CRM-систему для управления тендерами с интеграцией AI/RAG/KAG, превосходящую конкурента по функциональности, UX и автоматизации.

## Ключевые проблемы конкурента

| Проблема | Влияние | Наше решение |
|----------|---------|--------------|
| **26 колонок в таблице** | Невозможно работать | Настраиваемые представления |
| **Нет AI** | Ручная работа 2+ часа/день | GPT-4 автоматизация |
| **Нет графиков** | Сложно анализировать | Интерактивные дашборды |
| **Нет мобильной версии** | Нельзя работать в поле | PWA приложение |

## Наши преимущества

✅ **AI-автоматизация:** категоризация, прогнозирование, поиск  
✅ **Визуализация:** дашборды с графиками, воронки конверсии  
✅ **Настраиваемость:** гибкие таблицы, сохраненные фильтры  
✅ **Мобильность:** PWA, работа оффлайн  
✅ **Производительность:** < 2s загрузка, < 500ms API

## ROI

- **Экономия времени:** 2+ часа/день на менеджера
- **Увеличение конверсии:** +15% (просчет → подача), +10% (подача → победа)
- **Окупаемость:** 2 месяца при 10 пользователях

---

# Анализ недостатков конкурента

## 1. Проблемы UI/UX

### Перегруженность таблиц

- **Страница "Тендеры"**: 26 колонок
- **Страница "Задачи"**: 17 колонок
- **Отчёт по выплатам**: 21 колонка

**Решение:**
- Настраиваемые представления (presets)
- Закрепление первых 3-4 колонок
- Компактный/детальный режим

### Отсутствие визуализации

- Нет графиков и диаграмм
- Только табличные данные
- Нет дашбордов

**Решение:**
- Интеграция Chart.js
- Дашборды с KPI-виджетами
- Воронка конверсии

## 2. Функциональные пробелы

### Модуль "Логистика"

❌ Нет статусов доставки  
❌ Нет загрузки документов  
❌ Нет GPS-трекинга

### Модуль "Задачи"

❌ Нет кнопки "Добавить задачу"  
❌ Нет приоритета задачи  
❌ Нет уведомлений

### Модуль "Календарь"

❌ Нет цветовой кодировки  
❌ Нет drag-and-drop  
❌ Нет фильтров

## 3. Отсутствие AI/ML функций

❌ Автокатегоризация тендеров  
❌ Прогноз вероятности победы  
❌ Семантический поиск  
❌ Генерация саммари

## 4. Матрица приоритетов

### P0 — Критично для MVP

| Модуль | Улучшение |
|--------|-----------|
| Все таблицы | Настраиваемые представления |
| Дашборд | KPI-виджеты с графиками |
| Тендеры | AI-автокатегоризация |
| Задачи | Приоритеты и уведомления |

### P1 — Важно для конкурентного преимущества

| Модуль | Улучшение |
|--------|-----------|
| Тендеры | Прогноз вероятности победы |
| Логистика | Трекинг и статусы |
| Отчеты | Визуализация (графики) |

---

# AI/RAG/KAG Архитектура

## Архитектура системы

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (Next.js)                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Тендеры  │  │  Задачи  │  │ Отчеты   │  │ Поиск    │   │
└───────┼─────────────┼─────────────┼─────────────┼──────────┘
        │             │             │             │
┌───────▼─────────────▼─────────────▼─────────────▼──────────┐
│              AI Service Layer (lib/ai/)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   OpenAI     │  │  Embeddings  │  │   Prompts    │     │
│  │   Client     │  │   Generator  │  │   Templates  │     │
└─────────┼──────────────────┼──────────────────────────────┘
          │                  │
┌─────────▼──────────────────▼──────────────────────────────┐
│                 Supabase Database                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │  tenders     │  │ ai_summaries │  │  embeddings  │    │
│  │  (pgvector)  │  │              │  │   (vector)   │    │
└────────────────────────────────────────────────────────────┘
```

## RAG (Retrieval-Augmented Generation)

### Vector Store (pgvector)

```sql
CREATE TABLE tender_embeddings (
  id UUID PRIMARY KEY,
  tender_id UUID REFERENCES tenders(id),
  content TEXT,
  embedding VECTOR(1536),
  metadata JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX ON tender_embeddings 
USING ivfflat (embedding vector_cosine_ops);
```

### Embedding Pipeline

```typescript
export async function generateEmbedding(text: string) {
  const response = await openai.embeddings.create({
    model: "text-embedding-ada-002",
    input: text
  });
  return response.data[0].embedding;
}
```

### Semantic Search

```typescript
export async function semanticSearch(query: string, limit = 10) {
  const queryEmbedding = await generateEmbedding(query);
  
  const { data } = await supabase.rpc('match_tenders', {
    query_embedding: queryEmbedding,
    match_threshold: 0.78,
    match_count: limit
  });
  
  return data;
}
```

## KAG (Knowledge-Augmented Generation)

### Knowledge Graph Schema

```typescript
interface TenderKnowledgeGraph {
  entities: {
    tender: TenderNode;
    customer: CustomerNode;
    category: CategoryNode;
  };
  relations: {
    tender_belongs_to_category: Relation[];
    tender_has_customer: Relation[];
    similar_tenders: Relation[];
  };
}
```

## AI-функции

### Автокатегоризация тендеров

```typescript
export async function POST(req: Request) {
  const { title, description } = await req.json();
  
  const completion = await openai.chat.completions.create({
    model: "gpt-4-turbo-preview",
    messages: [
      {
        role: "system",
        content: `Ты эксперт по категоризации тендеров.
        Анализируй название и описание, выбери категорию.`
      },
      {
        role: "user",
        content: `Название: ${title}\nОписание: ${description}`
      }
    ],
    response_format: { type: "json_object" }
  });
  
  return Response.json(JSON.parse(completion.choices[0].message.content));
}
```

### Прогноз вероятности победы

```typescript
export async function predictWinProbability(tender: Tender) {
  const features = {
    nmck: tender.nmck,
    competition_level: await getCompetitionLevel(tender),
    our_experience: historicalData.total_wins,
    customer_loyalty: historicalData.customer_win_rate,
    price_competitiveness: tender.our_price / tender.nmck
  };
  
  const completion = await openai.chat.completions.create({
    model: "gpt-4-turbo-preview",
    messages: [
      {
        role: "system",
        content: `Оцени вероятность победы от 0 до 100%.`
      },
      {
        role: "user",
        content: JSON.stringify(features)
      }
    ]
  });
  
  return JSON.parse(completion.choices[0].message.content);
}
```

---

# Техническая спецификация

## Стек технологий

### Frontend

- **Framework**: Next.js 14+ (App Router)
- **Language**: TypeScript 5+
- **UI Components**: shadcn/ui + Radix UI
- **Styling**: TailwindCSS 3+
- **Charts**: Chart.js + react-chartjs-2
- **Forms**: React Hook Form + Zod
- **Drag & Drop**: @dnd-kit/core
- **Tables**: TanStack Table v8

### Backend

- **Database**: Supabase (PostgreSQL 15+)
- **Auth**: Supabase Auth
- **Storage**: Supabase Storage
- **Vector DB**: pgvector extension
- **Real-time**: Supabase Realtime

### AI/ML

- **LLM**: OpenAI GPT-4 Turbo
- **Embeddings**: text-embedding-ada-002
- **Vector Search**: pgvector + HNSW index
- **Cache**: Upstash Redis

### DevOps

- **Hosting**: Vercel
- **CI/CD**: GitHub Actions
- **Monitoring**: Sentry

## Основные таблицы БД

### Тендеры

```sql
CREATE TABLE tenders (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id),
  purchase_number TEXT NOT NULL UNIQUE,
  subject TEXT NOT NULL,
  customer TEXT NOT NULL,
  nmck BIGINT NOT NULL,
  our_price BIGINT,
  submission_deadline TIMESTAMPTZ NOT NULL,
  stage_id UUID NOT NULL REFERENCES tender_stages(id),
  status TEXT NOT NULL DEFAULT 'active',
  search_vector TSVECTOR GENERATED ALWAYS AS (
    to_tsvector('russian', 
      coalesce(purchase_number, '') || ' ' ||
      coalesce(subject, '') || ' ' ||
      coalesce(customer, '')
    )
  ) STORED,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Задачи

```sql
CREATE TABLE tasks (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id),
  tender_id UUID REFERENCES tenders(id),
  title TEXT NOT NULL,
  description TEXT,
  type TEXT,
  priority TEXT NOT NULL DEFAULT 'medium',
  assigned_to UUID NOT NULL REFERENCES profiles(id),
  status TEXT NOT NULL DEFAULT 'pending',
  due_date TIMESTAMPTZ,
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### AI: Embeddings

```sql
CREATE TABLE tender_embeddings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  tender_id UUID NOT NULL REFERENCES tenders(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  embedding VECTOR(1536),
  metadata JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_tender_embeddings_vector 
ON tender_embeddings USING ivfflat (embedding vector_cosine_ops)
WITH (lists = 100);
```

## API Routes структура

```
app/api/
├── tenders/
│   ├── route.ts              # GET, POST
│   ├── [id]/route.ts         # GET, PATCH, DELETE
│   └── import/route.ts       # POST (импорт из ЕИС)
├── tasks/
│   ├── route.ts
│   └── [id]/route.ts
├── ai/
│   ├── categorize/route.ts
│   ├── predict/route.ts
│   ├── search/route.ts
│   └── chat/route.ts
└── reports/
    ├── tender-dept/route.ts
    └── realization/route.ts
```

---

# UI/UX Концепция

## Дизайн-система

### Цветовая палитра

```typescript
export const colors = {
  status: {
    success: '#10b981',
    warning: '#f59e0b',
    error: '#ef4444',
    info: '#3b82f6',
  },
  priority: {
    urgent: '#dc2626',
    high: '#f97316',
    medium: '#eab308',
    low: '#22c55e',
  },
  stages: {
    analysis: '#8b5cf6',
    checking: '#ec4899',
    submission: '#3b82f6',
    won: '#10b981',
    lost: '#6b7280',
  }
};
```

## Ключевые UI-паттерны

### 1. Умная таблица с настройками

- Настройка видимости колонок
- Сортировка и фильтрация
- Экспорт в Excel/CSV

### 2. Kanban с drag-and-drop

- Перетаскивание между этапами
- Счетчики (сумма, количество)
- Цветовая кодировка

### 3. Дашборд с виджетами

- KPI-карточки
- Воронка конверсии
- Тренд побед
- Недавние тендеры

### 4. AI-ассистент

- Плавающая панель
- Чат с AI
- Контекстные подсказки

## Адаптивность

```typescript
export const breakpoints = {
  sm: '640px',   // Мобильные
  md: '768px',   // Планшеты
  lg: '1024px',  // Ноутбуки
  xl: '1280px',  // Десктопы
};
```

---

# План разработки

## Roadmap

### MVP (3 месяца)

| Sprint | Недели | Описание |
|--------|--------|----------|
| 0 | 1 | Подготовка |
| 1-2 | 2 | Аутентификация |
| 3-6 | 4 | Тендеры (базовый + kanban) |
| 7-8 | 2 | AI (базовый) |
| 11-12 | 2 | Задачи |
| 15-16 | 2 | Дашборд |

**Story Points:** 118

### Full version (7 месяцев)

- Sprint 9-10: AI (продвинутый)
- Sprint 13-14: Календарь
- Sprint 17-18: Реализация
- Sprint 19-20: Логистика
- Sprint 21-22: Взыскание долгов
- Sprint 23-28: Справочники, PWA, тестирование

**Story Points:** 270

## Стоимость

### Solo-разработчик

- **MVP (12 недель):** $24,000
- **Full version (29 недель):** $58,000

### Команда (2 человека)

- **MVP (6 недель):** $21,600
- **Full version (15 недель):** $54,000

### Дополнительные расходы (год)

| Сервис | Стоимость/год |
|--------|---------------|
| Supabase Pro | $300 |
| Vercel Pro | $240 |
| OpenAI API | $600-2,400 |
| **Итого** | **$1,275-3,075** |

## Метрики успеха

### Технические

- Lighthouse Score: 90+
- Test Coverage: 80%+
- API Response Time: < 500ms
- Page Load Time: < 2s

### Бизнес

- Время добавления тендера: < 2 мин
- Точность AI-категоризации: > 85%
- Экономия времени: 2+ часа/день
- Конверсия: +15%

## Риски

| Риск | Митигация |
|------|-----------|
| API ЕИС нестабилен | Кэширование, fallback |
| OpenAI дорого | Кэширование, лимиты |
| Перегрузка функционалом | Фокус на MVP |

---

## Сравнительная таблица

| Функция | Конкурент | Наша система |
|---------|-----------|--------------|
| AI-категоризация | ❌ | ✅ GPT-4 (85%+ точность) |
| Прогноз победы | ❌ | ✅ ML-модель |
| Семантический поиск | ❌ | ✅ pgvector + embeddings |
| Графики/дашборды | ❌ | ✅ Chart.js |
| Настройка таблиц | ❌ | ✅ Скрытие колонок |
| Drag-and-drop | ❌ | ✅ @dnd-kit |
| Мобильная версия | ❌ | ✅ PWA |
| API | ❌ | ✅ REST |

---

**Готово к разработке!** 🚀
