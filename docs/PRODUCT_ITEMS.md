# 📦 Справочник товаров — Полная документация

> **Статус:** ✅ Production Ready

---

## 📋 Содержание

1. [Описание](#описание)
2. [База данных](#база-данных)
3. [Компоненты](#компоненты)
4. [API](#api)
5. [Использование](#использование)
6. [Связь с транзакциями](#связь-с-транзакциями)
7. [Автоматическая синхронизация](#автоматическая-синхронизация)

---

## Описание

Справочник товаров позволяет:
- ✅ Создавать и управлять списком часто покупаемых товаров
- ✅ Автодополнение при добавлении позиций в транзакции
- ✅ Автоматическая синхронизация данных при изменении товара
- ✅ Связь позиций транзакций с товарами из справочника

**Преимущества:**
- При изменении названия товара → обновляется во всех транзакциях
- При изменении единицы измерения → обновляется везде
- При изменении категории товара → обновляется во всех транзакциях
- Единый источник данных о товарах

---

## База данных

### Таблица `product_items`

```sql
- id: UUID (PK)
- user_id: UUID (FK -> auth.users)
- name: TEXT (название товара)
- default_unit: TEXT (единица измерения)
- default_price_per_unit: BIGINT (цена в копейках, nullable)
- category_id: UUID (FK -> categories, nullable)
- description: TEXT (описание, nullable)
- is_active: BOOLEAN (активность)
- created_at: TIMESTAMPTZ
- updated_at: TIMESTAMPTZ
```

**RLS политики:** Пользователи видят и управляют только своими товарами.

### Связь с транзакциями

```sql
ALTER TABLE transaction_items
ADD COLUMN product_id uuid REFERENCES product_items(id) ON DELETE SET NULL;
```

- `product_id` — ссылка на товар из справочника (nullable)
- При удалении товара связь обнуляется (ON DELETE SET NULL)

### VIEW `transaction_items_with_products`

```sql
CREATE OR REPLACE VIEW transaction_items_with_products AS
SELECT 
  ti.id,
  ti.transaction_id,
  ti.user_id,
  ti.product_id,
  COALESCE(pi.name, ti.name) as name,
  ti.quantity,
  COALESCE(pi.default_unit, ti.unit) as unit,
  ti.price_per_unit,
  ti.total_amount,
  COALESCE(pi.category_id, ti.category_id) as category_id,
  ti.created_at,
  ti.updated_at,
  pi.name as product_name,
  c.name as category_name
FROM transaction_items ti
LEFT JOIN product_items pi ON ti.product_id = pi.id AND pi.is_active = true
LEFT JOIN categories c ON COALESCE(pi.category_id, ti.category_id) = c.id;
```

---

## Компоненты

### ProductItemsManager

`components/product-items/ProductItemsManager.tsx`

- Управление справочником товаров
- CRUD операции
- Таблица товаров
- Форма добавления/редактирования

### ProductAutocomplete

`components/transactions/ProductAutocomplete.tsx`

- Поле ввода с автодополнением
- Поиск товаров по названию (debounce 300ms)
- Навигация клавиатурой (↑↓, Enter, Esc)
- Отображение подсказок с ценой и категорией

### TransactionItems

- Интеграция с `ProductAutocomplete`
- Автоматическое заполнение при выборе товара:
  - Название
  - Единица измерения
  - Цена по умолчанию
  - Категория

---

## API

### Server Actions (`lib/product-items/service.ts`)

```typescript
// Получить список товаров
getProductItems(activeOnly: boolean)

// Поиск товаров (для автодополнения)
searchProductItems(query: string)

// Создать товар
createProductItem(input: ProductItemInput)

// Обновить товар
updateProductItem(update: ProductItemUpdate)

// Мягкое удаление (деактивация)
deleteProductItem(id: string)

// Физическое удаление
permanentDeleteProductItem(id: string)
```

---

## Использование

### Страница управления

**URL:** `/finance/settings/products`

Доступ: Настройки → Справочник товаров → Управление товарами

### Добавление товара

1. Перейти в Настройки → Управление товарами
2. Заполнить:
   - **Название** (обязательно)
   - **Единица измерения** (обязательно): шт, кг, л, г, мл, упак, м, м²
   - **Цена за единицу** (опционально)
   - **Категория** (опционально) — из существующих категорий
   - **Описание** (опционально)
3. Нажать "Добавить"

### Использование в транзакциях

1. Создать новую транзакцию
2. Отметить чекбокс "Добавить позиции товаров"
3. Начать вводить название товара
4. Выбрать товар из выпадающего списка
5. Автоматически заполнятся:
   - Единица измерения
   - Цена за единицу
   - Категория товара
6. При необходимости изменить количество или цену
7. Нажать "Добавить"

### Особенности автодополнения

- Поиск начинается после ввода 2+ символов
- Кнопка со стрелкой показывает все товары
- Debounce 300ms для оптимизации
- Поддержка навигации клавиатурой
- Валидация: только товары из справочника
- Визуальная индикация невалидного товара

---

## Связь с транзакциями

### Создание позиций с привязкой

```typescript
import { createTransactionItems } from "@/lib/transactions/transaction-items-service";

const items: TransactionItemInput[] = [
  {
    name: "Молоко",
    quantity: 2,
    unit: "л",
    price_per_unit: 8500, // в копейках
    product_id: "uuid-товара-из-справочника" // ВАЖНО!
  }
];

await createTransactionItems(transactionId, items);
```

### Получение позиций с актуальными данными

```typescript
import { getTransactionItems } from "@/lib/transactions/transaction-items-service";

const items = await getTransactionItems(transactionId);
// items[0].name - актуальное название из справочника
// items[0].unit - актуальная единица измерения
// items[0].category_name - актуальная категория
```

---

## Автоматическая синхронизация

### Изменение названия товара

```sql
UPDATE product_items SET name = 'Картошка' WHERE id = 'xxx';
-- Результат: во ВСЕХ транзакциях отображается "Картошка"
```

### Изменение единицы измерения

```sql
UPDATE product_items SET default_unit = 'л' WHERE name = 'Молоко';
-- Результат: во ВСЕХ транзакциях отображается "л"
```

### Изменение категории

```sql
UPDATE product_items 
SET category_id = (SELECT id FROM categories WHERE name = 'Продукты')
WHERE name = 'Хлеб';
-- Результат: во ВСЕХ транзакциях хлеб в категории "Продукты"
```

---

## Важные моменты

1. **Обратная совместимость:** Старые записи без `product_id` работают
2. **Удаление товара:** `product_id` обнуляется (ON DELETE SET NULL)
3. **Неактивные товары:** VIEW игнорирует неактивные (`is_active = true`)
4. **Приоритет данных:** Если есть `product_id` → данные из справочника

---

## Типы

```typescript
type ProductItem = {
  id: string;
  user_id: string;
  name: string;
  default_unit: string;
  default_price_per_unit: number | null;
  category_id: string | null;
  description: string | null;
  is_active: boolean;
  created_at: string;
  updated_at: string;
};

type ProductItemInput = {
  name: string;
  default_unit: string;
  default_price_per_unit?: number | null;
  category_id?: string | null;
  description?: string | null;
  is_active?: boolean;
};
```

---

## Миграции

```bash
# Применить миграции
supabase db push
```

**Файлы:**
- `db/migrations/20251106_create_product_items.sql`
- `db/migrations/20251113_add_product_id_to_transaction_items.sql`
- `db/migrations/20251113_create_transaction_items_view.sql`

---

## Roadmap

- [ ] Импорт товаров из CSV
- [ ] Экспорт справочника
- [ ] Группировка товаров по категориям
- [ ] История изменения цен
- [ ] Статистика по товарам
- [ ] Избранные товары
- [ ] Штрихкоды товаров

---

**Справочник товаров готов к использованию!** 🚀
