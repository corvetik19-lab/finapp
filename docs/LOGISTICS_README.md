# 🚚 Система управления логистикой

Полнофункциональная система управления отправками и доставками в стиле вашего приложения.

## 📋 Возможности

### ✨ Основной функционал
- **Управление отправками**: создание, редактирование, удаление отправок
- **Трекинг статусов**: отслеживание статуса доставки в реальном времени
- **Управление водителями**: добавление водителей и транспорта
- **Автоматическая генерация трек-номеров**: уникальные номера для каждой отправки
- **История изменений**: полная история изменения статусов
- **Фильтрация и поиск**: быстрый поиск по различным параметрам

### 📊 Статусы отправок
1. **Черновик** - отправка в процессе создания
2. **Подтверждено** - отправка подтверждена
3. **Забрано** - груз забран у отправителя
4. **В пути** - груз в процессе доставки
5. **Доставлено** - груз успешно доставлен
6. **Отменено** - отправка отменена
7. **Возврат** - груз возвращается отправителю

### 📦 Типы доставки
- **Обычная** - стандартная доставка
- **Экспресс** ⚡ - ускоренная доставка
- **За ночь** 🌙 - доставка за ночь
- **Грузоперевозки** 📦 - перевозка крупногабаритных грузов

## 🗄️ Структура базы данных

### Таблица `shipments` (отправки)
```sql
- id: uuid (PK)
- user_id: uuid (FK)
- tracking_number: text (уникальный трек-номер)
- status: text (статус отправки)
- type: text (тип доставки)
- sender_* : информация об отправителе
- recipient_*: информация о получателе
- description: text (описание груза)
- weight_kg, dimensions: параметры груза
- cost_amount: bigint (стоимость в копейках)
- pickup_date, delivery_date: даты
- driver_id: uuid (FK к водителю)
```

### Таблица `drivers` (водители)
```sql
- id: uuid (PK)
- user_id: uuid (FK)
- name: text
- phone: text
- license_number: text
- vehicle_*: информация о транспорте
- is_active: boolean
```

### Таблица `shipment_status_history` (история статусов)
```sql
- id: uuid (PK)
- shipment_id: uuid (FK)
- status: text
- timestamp: timestamptz
- location: text
- notes: text
```

## 🎨 UI Компоненты

### ShipmentsManager
Главный компонент управления отправками:
- Статистика по отправкам
- Кнопка создания новой отправки
- Интеграция с таблицей и формой

### ShipmentsTable
Таблица отправок с функциями:
- Фильтрация по статусам (табы)
- Поиск по всем полям
- Изменение статуса прямо в таблице
- Просмотр деталей
- Удаление отправок

### ShipmentFormModal
Модальная форма создания/редактирования:
- Выбор типа доставки
- Данные отправителя и получателя
- Адреса с координатами
- Информация о грузе (вес, размеры, стоимость)
- Выбор водителя
- Даты забора и доставки
- Особые указания

## 🚀 Использование

### 1. Применить миграцию
```bash
# Выполните SQL миграцию в Supabase
# Файл: db/migrations/0103_create_logistics_tables.sql
```

### 2. Доступ к странице
```
/logistics
```

### 3. Создание отправки
1. Нажмите кнопку "Новая отправка"
2. Выберите тип доставки
3. Заполните данные отправителя и получателя
4. Укажите информацию о грузе
5. Выберите водителя (опционально)
6. Нажмите "Создать отправку"

### 4. Управление статусами
- Используйте выпадающий список в таблице для изменения статуса
- История изменений сохраняется автоматически

## 🔧 API Endpoints

### GET /api/logistics/shipments
Получить список всех отправок

### POST /api/logistics/shipments
Создать новую отправку
```json
{
  "type": "express",
  "sender_name": "Иван Иванов",
  "sender_street": "ул. Ленина, 1",
  "sender_city": "Москва",
  ...
}
```

### GET /api/logistics/shipments/[id]
Получить детали отправки

### PATCH /api/logistics/shipments/[id]
Обновить статус отправки
```json
{
  "status": "in_transit",
  "notes": "Груз в пути"
}
```

### DELETE /api/logistics/shipments/[id]
Удалить отправку (мягкое удаление)

## 🎯 Особенности реализации

### Автоматическая генерация трек-номеров
```sql
-- Формат: TRK + YYMMDD + 4 цифры
-- Пример: TRK2511190001
```

### RLS (Row Level Security)
Все данные защищены политиками RLS:
- Пользователь видит только свои отправки
- Автоматическая фильтрация по user_id

### Триггеры
1. **auto_tracking_number** - автоматическая генерация трек-номера
2. **update_updated_at** - обновление timestamp
3. **shipment_status_history_trigger** - автоматическое добавление в историю

### Типизация
Полная типизация TypeScript:
- `Shipment` - тип отправки
- `Driver` - тип водителя
- `ShipmentStatus` - статусы
- `ShipmentType` - типы доставки

## 🎨 Стилизация

### Цветовая схема статусов
```typescript
{
  draft: '#94a3b8',      // Серый
  confirmed: '#3b82f6',  // Синий
  picked_up: '#f59e0b',  // Оранжевый
  in_transit: '#8b5cf6', // Фиолетовый
  delivered: '#10b981',  // Зелёный
  cancelled: '#ef4444',  // Красный
  returned: '#f97316',   // Оранжево-красный
}
```

### Адаптивный дизайн
- Полная поддержка мобильных устройств
- Горизонтальная прокрутка таблицы
- Адаптивные формы

## 📱 Мобильная версия
- Оптимизированная таблица с прокруткой
- Адаптивные формы
- Touch-friendly интерфейс

## 🔐 Безопасность
- RLS на уровне базы данных
- Валидация данных через Zod
- Защита от SQL-инъекций
- Авторизация через Supabase Auth

## 📈 Будущие улучшения
- [ ] Интеграция с картами (Google Maps / Yandex Maps)
- [ ] Push-уведомления о изменении статуса
- [ ] Печать накладных
- [ ] Экспорт в Excel/PDF
- [ ] Интеграция с курьерскими службами API
- [ ] Калькулятор стоимости доставки
- [ ] QR-коды для отправок
- [ ] Мобильное приложение для водителей

## 🤝 Интеграция с другими модулями
- **Тендеры**: связь отправок с тендерами
- **Финансы**: учёт расходов на доставку
- **Клиенты**: история отправок клиента

## 📝 Примеры использования

### Создание отправки через API
```typescript
const shipment = await createShipment({
  type: 'express',
  sender_name: 'ООО "Компания"',
  sender_street: 'ул. Ленина, 1',
  sender_city: 'Москва',
  sender_country: 'Россия',
  recipient_name: 'Иван Иванов',
  recipient_street: 'пр. Мира, 10',
  recipient_city: 'Санкт-Петербург',
  recipient_country: 'Россия',
  description: 'Документы',
  weight_kg: 0.5,
  cost_amount: 500, // в рублях
  currency: 'RUB',
});
```

### Изменение статуса
```typescript
await updateShipmentStatus(
  shipmentId, 
  'in_transit',
  'Груз покинул склад в Москве'
);
```

### Получение истории
```typescript
const history = await getShipmentHistory(shipmentId);
```

## 🎓 Обучение
Система интуитивно понятна и не требует специального обучения. Все поля имеют подсказки и валидацию.

---

**Создано с ❤️ для эффективного управления логистикой**
