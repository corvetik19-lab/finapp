# План разработки бухгалтерского модуля

> **Модуль:** Бухгалтерия (внутри режима "Тендеры")  
> **Доступ:** Super Admin, Admin организации  
> **Статус:** ✅ Базовая структура создана (Этап 1-3 выполнены)

---

## 📋 Содержание

1. [Обзор модуля](#обзор-модуля)
2. [Структура каталогов](#структура-каталогов)
3. [База данных](#база-данных)
4. [Этапы разработки](#этапы-разработки)
5. [Функционал по разделам](#функционал-по-разделам)

---

## Обзор модуля

### Цели

- Полноценный бухгалтерский учёт для организаций
- Формирование первичных документов (накладные, акты, счета, счёт-фактуры)
- Ведение КУДиР (Книга учёта доходов и расходов)
- Расчёт налогов с учётом формы организации (ИП/ООО) и режима налогообложения
- Календарь оплаты налогов с напоминаниями
- Интеграция с тендерами

### Поддерживаемые формы организаций

- **ИП** (Индивидуальный предприниматель)
- **ООО** (Общество с ограниченной ответственностью)
- **АО** (Акционерное общество)

### Режимы налогообложения

- **ОСНО** — Общая система налогообложения (с НДС)
- **УСН Доходы** — Упрощённая система (6%)
- **УСН Доходы-Расходы** — Упрощённая система (15%)
- **ПСН** — Патентная система (только ИП)
- **АУСН** — Автоматизированная УСН

---

## Структура каталогов

app/(protected)/tenders/accounting/
├── page.tsx                           # Дашборд бухгалтерии
├── layout.tsx                         # Layout с проверкой доступа
├── settings/
│   └── page.tsx                       # Настройки организации (ИП/ООО, НДС)
├── documents/
│   ├── page.tsx                       # Список всех документов
│   ├── invoices/                      # Счета на оплату
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [id]/page.tsx
│   ├── acts/                          # Акты выполненных работ
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [id]/page.tsx
│   ├── waybills/                      # Товарные накладные (ТОРГ-12)
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [id]/page.tsx
│   ├── upd/                           # УПД (универсальный передаточный документ)
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [id]/page.tsx
│   └── contracts/                     # Договоры
│       ├── page.tsx
│       ├── new/page.tsx
│       └── [id]/page.tsx
├── kudir/
│   └── page.tsx                       # Книга учёта доходов и расходов
├── taxes/
│   ├── page.tsx                       # Расчёт налогов
│   └── calendar/page.tsx              # Календарь оплаты налогов
├── reports/
│   ├── page.tsx                       # Список отчётов
│   ├── income-expense/page.tsx        # Отчёт доходы/расходы
│   ├── vat/page.tsx                   # Отчёт по НДС
│   └── profit-loss/page.tsx           # Отчёт прибыли/убытки
└── counterparties/
    └── page.tsx                       # Контрагенты

components/accounting/
├── AccountingLayout.tsx               # Layout модуля
├── AccountingSidebar.tsx              # Боковое меню
├── settings/
│   ├── OrganizationSettingsForm.tsx   # Форма настроек организации
│   └── TaxSystemSelector.tsx          # Выбор системы налогообложения
├── documents/
│   ├── DocumentsList.tsx              # Список документов
│   ├── DocumentForm.tsx               # Форма документа
│   ├── DocumentPreview.tsx            # Предпросмотр документа
│   ├── DocumentPDF.tsx                # Генерация PDF
│   ├── InvoiceForm.tsx                # Форма счёта
│   ├── ActForm.tsx                    # Форма акта
│   ├── WaybillForm.tsx                # Форма накладной
│   └── UPDForm.tsx                    # Форма УПД
├── kudir/
│   ├── KudirTable.tsx                 # Таблица КУДиР
│   ├── KudirEntryForm.tsx             # Форма записи
│   └── KudirExport.tsx                # Экспорт КУДиР
├── taxes/
│   ├── TaxCalculator.tsx              # Калькулятор налогов
│   ├── TaxCalendar.tsx                # Календарь налогов
│   └── TaxPaymentCard.tsx             # Карточка платежа
└── reports/
    ├── IncomeExpenseReport.tsx        # Отчёт доходы/расходы
    ├── VATReport.tsx                  # Отчёт НДС
    └── ProfitLossReport.tsx           # Отчёт прибыли/убытки

lib/accounting/
├── types.ts                           # Типы TypeScript
├── service.ts                         # CRUD операции
├── documents-service.ts               # Сервис документов
├── kudir-service.ts                   # Сервис КУДиР
├── tax-calculator.ts                  # Расчёт налогов
├── tax-calendar.ts                    # Календарь налогов
├── pdf-generator.ts                   # Генерация PDF
├── validation.ts                      # Валидация Zod
└── constants.ts                       # Константы (ставки налогов и т.д.)

## База данных

### Таблицы

#### 1. `accounting_settings` — Настройки бухгалтерии организации

```sql
CREATE TABLE accounting_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  
  -- Форма организации
  organization_type TEXT NOT NULL DEFAULT 'ooo', -- 'ip', 'ooo', 'ao'
  
  -- Реквизиты
  full_name TEXT NOT NULL,                    -- Полное наименование
  short_name TEXT,                            -- Краткое наименование
  inn TEXT NOT NULL,                          -- ИНН
  kpp TEXT,                                   -- КПП (для ООО/АО)
  ogrn TEXT,                                  -- ОГРН/ОГРНИП
  okpo TEXT,                                  -- ОКПО
  okved TEXT,                                 -- Основной ОКВЭД
  
  -- Адреса
  legal_address TEXT,                         -- Юридический адрес
  actual_address TEXT,                        -- Фактический адрес
  
  -- Банковские реквизиты
  bank_name TEXT,                             -- Наименование банка
  bank_bik TEXT,                              -- БИК
  bank_account TEXT,                          -- Расчётный счёт
  bank_corr_account TEXT,                     -- Корр. счёт
  
  -- Руководитель
  director_name TEXT,                         -- ФИО руководителя
  director_position TEXT DEFAULT 'Директор',  -- Должность
  accountant_name TEXT,                       -- ФИО бухгалтера
  
  -- Система налогообложения
  tax_system TEXT NOT NULL DEFAULT 'usn_income', -- 'osno', 'usn_income', 'usn_income_expense', 'psn', 'ausn'
  vat_payer BOOLEAN DEFAULT false,            -- Плательщик НДС
  vat_rate INTEGER DEFAULT 20,                -- Ставка НДС (0, 10, 20)
  
  -- УСН настройки
  usn_rate NUMERIC(5,2) DEFAULT 6,            -- Ставка УСН (может быть региональная)
  usn_min_tax_rate NUMERIC(5,2) DEFAULT 1,    -- Минимальный налог УСН (1%)
  
  -- Настройки нумерации документов
  invoice_prefix TEXT DEFAULT 'СЧ',           -- Префикс счетов
  invoice_next_number INTEGER DEFAULT 1,
  act_prefix TEXT DEFAULT 'АКТ',              -- Префикс актов
  act_next_number INTEGER DEFAULT 1,
  waybill_prefix TEXT DEFAULT 'ТН',           -- Префикс накладных
  waybill_next_number INTEGER DEFAULT 1,
  upd_prefix TEXT DEFAULT 'УПД',              -- Префикс УПД
  upd_next_number INTEGER DEFAULT 1,
  contract_prefix TEXT DEFAULT 'ДОГ',         -- Префикс договоров
  contract_next_number INTEGER DEFAULT 1,
  
  -- Настройки учёта
  fiscal_year_start INTEGER DEFAULT 1,        -- Начало финансового года (месяц)
  
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  
  UNIQUE(company_id)
);
```

#### 2. `accounting_documents` — Документы

```sql
CREATE TABLE accounting_documents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  created_by UUID NOT NULL REFERENCES auth.users(id),
  
  -- Тип документа
  document_type TEXT NOT NULL, -- 'invoice', 'act', 'waybill', 'upd', 'contract', 'invoice_factura'
  
  -- Номер и дата
  document_number TEXT NOT NULL,
  document_date DATE NOT NULL,
  
  -- Связь с тендером (опционально)
  tender_id UUID REFERENCES tenders(id) ON DELETE SET NULL,
  
  -- Контрагент
  counterparty_id UUID REFERENCES accounting_counterparties(id),
  counterparty_name TEXT NOT NULL,
  counterparty_inn TEXT,
  counterparty_kpp TEXT,
  counterparty_address TEXT,
  
  -- Суммы (в копейках)
  subtotal BIGINT NOT NULL DEFAULT 0,         -- Сумма без НДС
  vat_amount BIGINT NOT NULL DEFAULT 0,       -- Сумма НДС
  total BIGINT NOT NULL DEFAULT 0,            -- Итого с НДС
  
  vat_rate INTEGER,                           -- Ставка НДС
  
  -- Для договоров
  contract_start_date DATE,
  contract_end_date DATE,
  
  -- Статус
  status TEXT NOT NULL DEFAULT 'draft', -- 'draft', 'issued', 'paid', 'cancelled'
  paid_at TIMESTAMPTZ,
  paid_amount BIGINT DEFAULT 0,
  
  -- Дополнительно
  notes TEXT,
  metadata JSONB DEFAULT '{}',
  
  -- PDF
  pdf_path TEXT,
  
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  deleted_at TIMESTAMPTZ
);
```

#### 3. `accounting_document_items` — Позиции документов

```sql
CREATE TABLE accounting_document_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  document_id UUID NOT NULL REFERENCES accounting_documents(id) ON DELETE CASCADE,
  
  -- Позиция
  position INTEGER NOT NULL DEFAULT 1,
  
  -- Товар/услуга
  name TEXT NOT NULL,
  description TEXT,
  unit TEXT DEFAULT 'шт',                     -- Единица измерения
  quantity NUMERIC(15,3) NOT NULL DEFAULT 1,
  
  -- Цены (в копейках)
  price_per_unit BIGINT NOT NULL,             -- Цена за единицу без НДС
  vat_rate INTEGER DEFAULT 0,                 -- Ставка НДС для позиции
  vat_amount BIGINT NOT NULL DEFAULT 0,       -- Сумма НДС
  total BIGINT NOT NULL,                      -- Итого с НДС
  
  created_at TIMESTAMPTZ DEFAULT now()
);
```

#### 4. `accounting_counterparties` — Контрагенты

```sql
CREATE TABLE accounting_counterparties (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  
  -- Основное
  name TEXT NOT NULL,
  short_name TEXT,
  organization_type TEXT DEFAULT 'ooo',       -- 'ip', 'ooo', 'ao', 'budget'
  
  -- Реквизиты
  inn TEXT,
  kpp TEXT,
  ogrn TEXT,
  
  -- Адреса
  legal_address TEXT,
  actual_address TEXT,
  
  -- Банковские реквизиты
  bank_name TEXT,
  bank_bik TEXT,
  bank_account TEXT,
  bank_corr_account TEXT,
  
  -- Контакты
  contact_person TEXT,
  phone TEXT,
  email TEXT,
  
  -- Тип
  is_customer BOOLEAN DEFAULT true,           -- Заказчик
  is_supplier BOOLEAN DEFAULT false,          -- Поставщик
  
  -- Связь с справочником заказчиков тендеров
  tender_customer_id UUID REFERENCES tender_customers(id),
  
  notes TEXT,
  
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now(),
  deleted_at TIMESTAMPTZ
);
```

#### 5. `kudir_entries` — Записи КУДиР

```sql
CREATE TABLE kudir_entries (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  
  -- Номер и дата
  entry_number INTEGER NOT NULL,              -- Порядковый номер
  entry_date DATE NOT NULL,
  
  -- Связи
  document_id UUID REFERENCES accounting_documents(id) ON DELETE SET NULL,
  tender_id UUID REFERENCES tenders(id) ON DELETE SET NULL,
  
  -- Первичный документ
  primary_document_type TEXT,                 -- Тип документа-основания
  primary_document_number TEXT,               -- Номер документа
  primary_document_date DATE,                 -- Дата документа
  
  -- Содержание операции
  description TEXT NOT NULL,
  
  -- Суммы (в копейках)
  income BIGINT DEFAULT 0,                    -- Доходы
  expense BIGINT DEFAULT 0,                   -- Расходы
  
  -- Для УСН Доходы-Расходы: учитываемые расходы
  deductible_expense BIGINT DEFAULT 0,
  
  -- Метаданные
  is_manual BOOLEAN DEFAULT false,            -- Ручная запись
  metadata JSONB DEFAULT '{}',
  
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

#### 6. `tax_payments` — Налоговые платежи

```sql
CREATE TABLE tax_payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  
  -- Налог
  tax_type TEXT NOT NULL,                     -- 'usn', 'vat', 'income_tax', 'property_tax', 'insurance'
  tax_name TEXT NOT NULL,                     -- Название налога
  
  -- Период
  period_year INTEGER NOT NULL,
  period_quarter INTEGER,                     -- 1-4, null для годовых
  period_month INTEGER,                       -- 1-12, null для квартальных/годовых
  
  -- Суммы (в копейках)
  calculated_amount BIGINT NOT NULL,          -- Рассчитанная сумма
  paid_amount BIGINT DEFAULT 0,               -- Оплаченная сумма
  
  -- Сроки
  due_date DATE NOT NULL,                     -- Срок оплаты
  paid_at TIMESTAMPTZ,                        -- Дата оплаты
  
  -- Статус
  status TEXT NOT NULL DEFAULT 'pending',     -- 'pending', 'paid', 'overdue', 'partial'
  
  -- Расчёт
  calculation_details JSONB DEFAULT '{}',     -- Детали расчёта
  
  notes TEXT,
  
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

#### 7. `tax_calendar_events` — События налогового календаря

```sql
CREATE TABLE tax_calendar_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE,
  
  -- Событие
  event_type TEXT NOT NULL,                   -- 'payment', 'report', 'deadline'
  title TEXT NOT NULL,
  description TEXT,
  
  -- Дата
  event_date DATE NOT NULL,
  
  -- Связь с платежом
  tax_payment_id UUID REFERENCES tax_payments(id) ON DELETE CASCADE,
  
  -- Напоминание
  reminder_days INTEGER DEFAULT 3,            -- За сколько дней напоминать
  reminder_sent BOOLEAN DEFAULT false,
  
  -- Статус
  is_completed BOOLEAN DEFAULT false,
  completed_at TIMESTAMPTZ,
  
  created_at TIMESTAMPTZ DEFAULT now()
);
```

---

## Этапы разработки

### Этап 1: Фундамент ✅

- [x] 1.1 Создать план разработки (этот документ)
- [x] 1.2 Миграции БД (`20251213_create_accounting_system.sql`, `20241213_add_tax_payments.sql`)
- [x] 1.3 Типы TypeScript (`lib/accounting/types.ts`)
- [x] 1.4 Модуль в навигации (`app/(protected)/tenders/accounting/`)
- [x] 1.5 Layout с проверкой доступа (`layout.tsx` — admin/super_admin)
- [x] 1.6 Страница-дашборд (`AccountingDashboard`)

### Этап 2: Настройки организации ✅

- [x] 2.1 Сервис настроек (`lib/accounting/settings-service.ts`)
- [x] 2.2 Форма настройки организации (OrganizationSettingsForm)
- [x] 2.3 Выбор системы налогообложения (ОСНО/УСН/ЕСХН/ПСН)
- [x] 2.4 Настройка НДС (плательщик/не плательщик, ставка)
- [x] 2.5 Реквизиты организации (ИНН, КПП, ОГРН, адреса)
- [x] 2.6 Банковские реквизиты (БИК, р/с, к/с)
- [x] 2.7 Настройка нумерации документов (префиксы)
- [x] 2.8 API настроек (`/api/accounting/settings`)

### Этап 3: Контрагенты ✅

- [x] 3.1 Сервис контрагентов (`lib/accounting/counterparty-service.ts`)
- [x] 3.2 CRUD контрагентов (create/update/delete)
- [x] 3.3 Импорт из тендеров (`importCounterpartiesFromTenders`)
- [x] 3.4 Автозаполнение по ИНН (`searchByInn` через DaData API)
- [x] 3.5 Фильтрация и поиск (`getCounterpartiesList`)
- [x] 3.6 API контрагентов (`/api/accounting/counterparties`)

### Этап 4: Документооборот ✅

- [x] 4.1 Сервис форм документов (`lib/accounting/document-forms-service.ts`)
- [x] 4.2 Универсальная форма документа (UniversalDocumentForm)
  - [x] 4.2.1 Форма создания/редактирования с позициями
  - [x] 4.2.2 Расчёт сумм и НДС
  - [x] 4.2.3 Выбор контрагента
- [x] 4.3 Типы документов (счёт, акт, СФ, УПД, накладная, платёжка, договор)
- [x] 4.4 Генерация номеров документов (`getNextDocumentNumber`)
- [x] 4.5 API документов (`/api/accounting/documents`)
- [x] 4.6 Получение документа для формы (`getDocumentForForm`)
- [x] 4.7 Создание документа (`createDocument`)
- [x] 4.8 Обновление документа (`updateDocument`)
- [x] 4.9 Статусы документов (черновик, выставлен, оплачен, отменён)
- [x] 4.10 Копирование документов (`copyDocument`)

### Этап 5: КУДиР ✅

- [x] 5.1 Страница КУДиР `/tenders/accounting/kudir` (KudirPage)
- [x] 5.2 Автоматическое заполнение из документов (`syncKudirFromDocuments`)
- [x] 5.3 Ручное добавление записей (`createKudirEntryManual`)
- [x] 5.4 Фильтрация по периодам (год/квартал/месяц/тип)
- [x] 5.5 Расчёт итогов по кварталам (`getKudirQuarterSummaries`)
- [x] 5.6 Расчёт итогов по году (`getKudirYearSummary`)
- [x] 5.7 API КУДиР (`/api/accounting/kudir`)
- [x] 5.8 Экспорт данных (`getKudirExportData`)

### Этап 6: Расчёт налогов ✅

- [x] 6.1 Страница калькуляторов `/tenders/accounting/taxes/calculators` (TaxCalculatorsPage)
- [x] 6.2 Калькулятор УСН Доходы (6%) (`calculateUsn6`)
  - [x] 6.2.1 Расчёт авансовых платежей по кварталам
  - [x] 6.2.2 Уменьшение на страховые взносы (100% для ИП, 50% с сотр.)
  - [x] 6.2.3 Расчёт годового налога
- [x] 6.3 Калькулятор УСН Доходы-Расходы (15%) (`calculateUsn15`)
  - [x] 6.3.1 Расчёт авансовых платежей по кварталам
  - [x] 6.3.2 Минимальный налог (1%)
  - [x] 6.3.3 Расчёт годового налога
- [x] 6.4 Калькулятор НДС (`calculateVat`)
  - [x] 6.4.1 НДС к уплате (исходящий)
  - [x] 6.4.2 НДС к вычету (входящий)
  - [x] 6.4.3 Итого НДС / к возмещению
- [x] 6.5 Расчёт страховых взносов ИП (`calculateIpInsurance`)
  - [x] 6.5.1 Фиксированные взносы (49 437 ₽)
  - [x] 6.5.2 1% с дохода свыше 300 000 ₽
- [x] 6.6 Расчёт страховых взносов за сотрудников (`calculateEmployeeInsurance`)
- [x] 6.7 API калькуляторов (`/api/accounting/tax-calculator`)

### Этап 7: Календарь налогов ✅

- [x] 7.1 Страница календаря `/tenders/accounting/taxes/calendar` (TaxCalendarPage)
- [x] 7.2 Отображение сроков оплаты (`getTaxCalendar`)
- [x] 7.3 Напоминания о сроках (`getUpcomingTaxPayments`)
- [x] 7.4 Отметка оплаченных налогов (`markTaxPaymentPaid`)
- [x] 7.5 Предстоящие платежи на дашборде (`getUpcomingTaxPayments`)
- [x] 7.6 API календаря налогов (`/api/accounting/tax-calendar`)
- [x] 7.7 Таблица `tax_payments` (миграция)

### Этап 8: Отчёты ✅

- [x] 8.1 Страница отчётов `/tenders/accounting/reports` (AccountingReportsPage)
- [x] 8.2 Отчёт Доходы/Расходы (`getIncomeExpenseReport`)
- [x] 8.3 Отчёт по НДС (`getVatReport`)
- [x] 8.4 Отчёт Прибыли/Убытки (`getProfitLossReport`)
- [x] 8.5 Отчёт по контрагентам (`getCounterpartyReport`)
- [x] 8.6 Отчёт по тендерам (`getTenderReport`)
- [x] 8.7 API для отчётов (`/api/accounting/reports`)

### Этап 9: Интеграции с тендерами ✅

- [x] 9.1 Создание документов из тендера (`lib/accounting/tender-integration.ts`)
- [x] 9.2 Автоматическое заполнение КУДиР при оплате тендера
- [x] 9.3 Связь документов с тендерами (tender_id в accounting_documents)
- [x] 9.4 Отчёты по тендерам в бухгалтерии (getTendersForAccountingReport)
- [x] 9.5 UI панель бухгалтерии на странице тендера (TenderAccountingPanel)
- [x] 9.6 API роуты для интеграции (`/api/accounting/tender/[tenderId]/*`)

### Этап 10: Банковские интеграции ✅

- [x] 10.1 Таблицы БД для банковских интеграций
  - [x] `bank_accounts` — расчётные счета организации
  - [x] `bank_integrations` — настройки API подключений к банкам
  - [x] `bank_transactions` — банковские транзакции (выписка)
  - [x] `payment_orders` — платёжные поручения
  - [x] `bank_sync_logs` — логи синхронизации
- [x] 10.2 Типы TypeScript для банковских сущностей
  - [x] `lib/accounting/bank-types.ts` — BankAccount, BankIntegration, PaymentOrder и др.
  - [x] Константы банков (Сбер, Тинькофф, Альфа, ВТБ, Точка, Модуль)
  - [x] Статусы, типы счетов, типы интеграций
- [x] 10.3 Сервисы для работы с банковскими данными
  - [x] `lib/accounting/bank-service.ts` — CRUD для всех банковских сущностей
  - [x] Server Actions для банковских счетов
  - [x] Server Actions для интеграций
- [x] 10.4 Страница расчётных счетов `/tenders/accounting/bank-accounts`
  - [x] 10.4.1 Список счетов с балансами
  - [x] 10.4.2 Добавление/редактирование счёта (BankAccountForm)
  - [x] 10.4.3 Просмотр выписки по счёту (BankTransactionsPage)
- [x] 10.5 Страница банковских интеграций `/tenders/accounting/bank-integrations`
  - [x] 10.5.1 Список подключённых банков
  - [x] 10.5.2 Мастер подключения нового банка (BankIntegrationForm)
  - [x] 10.5.3 OAuth авторизация (`lib/accounting/bank-oauth.ts`, API callback)
  - [x] 10.5.4 Настройки синхронизации (UI)
- [x] 10.6 Банковская выписка
  - [x] 10.6.1 Получение транзакций по API (`lib/accounting/bank-api-tinkoff.ts`)
  - [x] 10.6.2 Автоматическая категоризация (`lib/accounting/bank-categorization.ts`)
  - [x] 10.6.3 Связь с документами бухгалтерии (`lib/accounting/bank-document-linking.ts`)
  - [x] 10.6.4 Создание записей КУДиР (`lib/accounting/bank-kudir.ts`)
- [x] 10.7 Платёжные поручения
  - [x] 10.7.1 Форма создания платёжного поручения (PaymentOrderForm)
  - [x] 10.7.2 Отправка в банк через API (`lib/accounting/bank-payment-orders.ts`)
  - [x] 10.7.3 Отслеживание статуса (checkPaymentOrderStatus, syncPaymentOrderStatuses)
- [x] 10.8 API интеграции с банками
  - [x] 10.8.1 Тинькофф Бизнес API (`lib/accounting/bank-api-tinkoff.ts`)
  - [ ] 10.8.2 Сбербанк API (СберБизнес)
  - [ ] 10.8.3 Альфа-Банк API
  - [ ] 10.8.4 Точка Банк API
  - [ ] 10.8.5 Модульбанк API
- [x] 10.9 Автоматическая синхронизация (CRON)
  - [x] 10.9.1 Обновление балансов (`app/api/cron/bank-sync/route.ts`)
  - [x] 10.9.2 Получение новых транзакций
  - [x] 10.9.3 Обновление токенов

### Этап 11: Формы документов ✅

- [x] 11.1 DocumentForm — универсальная форма документа
  - [x] Типы документов (счёт, акт, накладная, УПД)
  - [x] Позиции с расчётом НДС
  - [x] Автоподсчёт итогов
- [x] 11.2 Навигация в бухгалтерии обновлена
  - [x] Добавлен пункт "Расчётные счета" в ModeSidebar

---

## Функционал по разделам

### 📊 Дашборд бухгалтерии

- Виджет "Доходы за период"
- Виджет "Расходы за период"
- Виджет "Прибыль"
- Виджет "Предстоящие налоги"
- Виджет "Неоплаченные счета"
- Виджет "Просроченные платежи"
- Быстрые действия (создать счёт, акт, накладную)

### ⚙️ Настройки организации

**Форма организации:**

- ИП (Индивидуальный предприниматель)
- ООО (Общество с ограниченной ответственностью)
- АО (Акционерное общество)

**Система налогообложения:**

| Система | Описание | Ставка | Для кого |
|---------|----------|--------|----------|
| ОСНО | Общая система | НДС 20% + Налог на прибыль 20% | ООО, АО |
| УСН Доходы | Упрощённая | 6% от доходов | ИП, ООО |
| УСН Доходы-Расходы | Упрощённая | 15% от (доходы - расходы) | ИП, ООО |
| ПСН | Патент | Фиксированная | Только ИП |
| АУСН | Автоматизированная | 8% или 20% | ИП, ООО |

**Настройки НДС:**

- Плательщик НДС: Да/Нет
- Ставка НДС: 0%, 10%, 20%
- Раздельный учёт НДС

### 📄 Документы

**Типы документов:**

| Документ | Описание | PDF |
|----------|----------|-----|
| Счёт на оплату | Выставление счёта клиенту | ✅ |
| Акт | Акт выполненных работ/оказанных услуг | ✅ |
| Товарная накладная | ТОРГ-12 | ✅ |
| УПД | Универсальный передаточный документ | ✅ |
| Счёт-фактура | Для плательщиков НДС | ✅ |
| Договор | Договор с контрагентом | ✅ |

**Статусы документов:**

- 📝 Черновик
- 📤 Выставлен
- ✅ Оплачен
- ❌ Отменён

### 📖 КУДиР

**Разделы КУДиР:**

1. **Раздел I** — Доходы и расходы
2. **Раздел II** — Расчёт расходов на приобретение ОС
3. **Раздел III** — Расчёт суммы убытка
4. **Раздел IV** — Расходы на страховые взносы

**Автоматическое заполнение:**

- При выставлении счёта → запись дохода
- При оплате поставщику → запись расхода
- При оплате налогов → запись расхода

### 💰 Налоги

**Календарь налогов (УСН):**

| Период | Срок оплаты | Срок отчёта |
|--------|-------------|-------------|
| 1 квартал | 28 апреля | — |
| 2 квартал | 28 июля | — |
| 3 квартал | 28 октября | — |
| Год | 28 апреля (ООО) / 28 апреля (ИП) | 25 марта (ООО) / 25 апреля (ИП) |

**Страховые взносы ИП (2024):**

- Фиксированные: 49 500 ₽ (до 31 декабря)
- 1% с дохода > 300 000 ₽ (до 1 июля следующего года)

---

## Меню модуля

📊 Бухгалтерия
├── 📈 Дашборд
├── 📄 Документы
│   ├── Все документы
│   ├── Счета
│   ├── Акты
│   ├── Накладные
│   ├── УПД
│   └── Договоры
├── 📖 КУДиР
├── 💰 Налоги
│   ├── Расчёт налогов
│   └── Календарь платежей
├── 📊 Отчёты
│   ├── Доходы/Расходы
│   ├── НДС
│   └── Прибыли/Убытки
├── 👥 Контрагенты
└── ⚙️ Настройки

## Технические решения

| Задача | Решение |
|--------|---------|
| Генерация PDF | `@react-pdf/renderer` |
| Экспорт Excel | `xlsx` (SheetJS) |
| Нумерация документов | Счётчики в `accounting_settings` |
| Автозаполнение по ИНН | DaData API (опционально) |
| Хранение сумм | В копейках (BIGINT) |
| Права доступа | RLS + проверка роли в middleware |

---

## Этап 7: Расширенный функционал (В разработке)

### 7.1 ЭДО и Интеграции

- [x] 7.1.1 Интеграция с Диадок (отправка/получение УПД, Актов) ✅
- [x] 7.1.2 XML формат обмена для 1С Бухгалтерии ✅
- [x] 7.1.3 Импорт банковской выписки ✅

### 7.2 Управленческий учёт

- [x] 7.2.1 Отчёт ДДС (Движение денежных средств) ✅
- [x] 7.2.2 Платёжный календарь (прогноз кассового разрыва) ✅
- [x] 7.2.3 P&L по тендерам (маржинальность проектов) ✅
- [x] 7.2.4 Связь расходов с тендерами ✅

### 7.3 Дополнительные документы

- [x] 7.3.1 Акт сверки с контрагентами ✅
- [x] 7.3.2 Доверенности М-2 ✅
- [x] 7.3.3 ПКО/РКО (кассовые ордера) ✅
- [x] 7.3.4 Кассовая книга ✅
- [x] 7.3.5 Шаблоны договоров (конструктор) ✅

### 7.4 Профессиональная печать документов

- [x] 7.4.1 ТОРГ-12 с печатью организации ✅ (компонент Torg12PDF.tsx)
- [x] 7.4.2 Счёт-фактура с подписями ✅ (компонент InvoicePDF.tsx)
- [x] 7.4.3 УПД полный формат ✅ (компонент UPDPDF.tsx)
- [x] 7.4.4 Загрузка печати и подписи в настройках ✅ (вкладка "Печать" в настройках)

### 7.5 Дашборд бухгалтерии

- [x] 7.5.1 Виджет выручки по месяцам (график) ✅ (Chart.js Bar chart)
- [x] 7.5.2 Виджет расходов по категориям ✅ (в графике доходов/расходов)
- [x] 7.5.3 Виджет дебиторской задолженности ✅ (Doughnut chart + список)
- [x] 7.5.4 Виджет кредиторской задолженности ✅ (Doughnut chart + список)
- [x] 7.5.5 Виджет налоговой нагрузки ✅ (компонент TaxBurdenWidget.tsx)
- [x] 7.5.6 Топ контрагентов по обороту ✅ (список топ-5)

---

## Примечания

- Все суммы хранятся в **копейках** (BIGINT) для точности
- Даты хранятся в **UTC**, отображаются в локальной временной зоне
- Документы генерируются в **PDF** для печати
- **КУДиР** ведётся автоматически на основе документов
- **Налоги** рассчитываются автоматически на основе КУДиР
