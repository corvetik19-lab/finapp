# Исправление сохранения порядка этапов

## 🐛 Проблема

При изменении порядка этапов через drag-and-drop порядок менялся визуально, но после перезагрузки страницы возвращался обратно.

## 🔍 Причина

API endpoint `PATCH /api/tenders/stages/:id` требовал обязательное поле `name` при любом обновлении. При drag-and-drop мы отправляли только `order_index`, что вызывало ошибку 400 и обновление не сохранялось.

## ✅ Решение

### 1. Сделаны все поля опциональными в PATCH

**Файл:** `app/api/tenders/stages/[id]/route.ts`

**Было:**
```typescript
const { name, color, is_final, category, order_index, is_active } = body;

if (!name || !name.trim()) {
  return NextResponse.json(
    { error: 'Name is required' },
    { status: 400 }
  );
}

const updateData: Record<string, string | number | boolean> = {
  name: name.trim(),
  color: color || '#3b82f6',
  is_final: is_final ?? false,
};
```

**Стало:**
```typescript
const { name, color, is_final, category, order_index, is_active } = body;

const updateData: Record<string, string | number | boolean> = {};

// Добавляем поля только если они переданы
if (name !== undefined) {
  if (!name.trim()) {
    return NextResponse.json(
      { error: 'Name cannot be empty' },
      { status: 400 }
    );
  }
  updateData.name = name.trim();
}
if (color !== undefined) updateData.color = color;
if (is_final !== undefined) updateData.is_final = is_final;
if (category !== undefined) updateData.category = category;
if (order_index !== undefined) updateData.order_index = order_index;
if (is_active !== undefined) updateData.is_active = is_active;

// Проверяем что есть хотя бы одно поле для обновления
if (Object.keys(updateData).length === 0) {
  return NextResponse.json(
    { error: 'No fields to update' },
    { status: 400 }
  );
}
```

### 2. Добавлено логирование и обработка ошибок

**Файл:** `app/(protected)/tenders/settings/tender-settings-complete.tsx`

```typescript
// Отправляем обновления на сервер
try {
  console.log('Updating stage order:', updatedCategoryStages.map(s => ({ 
    id: s.id, 
    name: s.name, 
    order_index: s.order_index 
  })));
  
  const responses = await Promise.all(
    updatedCategoryStages.map(stage =>
      fetch(`/api/tenders/stages/${stage.id}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ order_index: stage.order_index }),
      })
    )
  );

  // Проверяем что все запросы успешны
  const failedResponses = responses.filter(r => !r.ok);
  if (failedResponses.length > 0) {
    console.error('Some updates failed:', failedResponses);
    throw new Error('Failed to update some stages');
  }

  console.log('Stage order updated successfully');
  notifyStagesUpdated();
} catch (error) {
  console.error('Error updating stage order:', error);
  toast.show('Ошибка при сохранении порядка этапов', { type: 'error' });
}
```

## 🧪 Как протестировать

### Тест 1: Изменение порядка

1. Откройте **Настройки тендеров** → **Этапы**
2. Перетащите любой этап на новую позицию
3. Откройте консоль браузера (F12)
4. Проверьте логи:
   ```
   Updating stage order: [{id: "...", name: "...", order_index: 1}, ...]
   Stage order updated successfully
   ```
5. **Перезагрузите страницу** (F5)
6. **Проверьте:** порядок должен сохраниться

### Тест 2: Проверка в канбан-доске

1. Откройте **Тендерный отдел** в одной вкладке
2. Откройте **Настройки этапов** в другой вкладке
3. Измените порядок этапов в настройках
4. **Проверьте:** порядок колонок в канбан-доске обновился
5. **Перезагрузите** страницу тендерного отдела
6. **Проверьте:** порядок колонок сохранился

### Тест 3: Обработка ошибок

1. Откройте консоль браузера (F12) → Network
2. Измените порядок этапов
3. Проверьте запросы `PATCH /api/tenders/stages/:id`
4. Все должны вернуть статус **200 OK**
5. Если есть ошибки - появится toast уведомление

## 📊 Что изменилось

**До исправления:**
- ❌ PATCH требовал обязательное поле `name`
- ❌ При отправке только `order_index` возвращалась ошибка 400
- ❌ Порядок не сохранялся в БД
- ❌ После перезагрузки порядок возвращался обратно

**После исправления:**
- ✅ PATCH принимает любые поля опционально
- ✅ Можно обновить только `order_index`
- ✅ Порядок сохраняется в БД
- ✅ После перезагрузки порядок остаётся
- ✅ Добавлено логирование для отладки
- ✅ Добавлена обработка ошибок с уведомлениями

## 🔧 Технические детали

### API Request при drag-and-drop

```http
PATCH /api/tenders/stages/uuid-этапа
Content-Type: application/json

{
  "order_index": 3
}
```

### API Response

```json
{
  "data": {
    "id": "uuid-этапа",
    "name": "Анализ и просчёт",
    "order_index": 3,
    "category": "tender_dept",
    ...
  },
  "message": "Tender stage updated successfully"
}
```

### Обновление в БД

```sql
UPDATE tender_stages 
SET order_index = 3, updated_at = NOW() 
WHERE id = 'uuid-этапа';
```

## ✅ Результат

Теперь порядок этапов:
- ✅ Сохраняется в базе данных
- ✅ Остаётся после перезагрузки страницы
- ✅ Синхронизируется между вкладками
- ✅ Отображается корректно в канбан-доске
- ✅ Имеет понятные логи для отладки
- ✅ Показывает ошибки пользователю
