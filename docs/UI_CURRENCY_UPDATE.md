# 💰 Обновление UI: Рубли вместо копеек

## Что изменено

### ✅ Форма тендера (`tender-form-modal.tsx`)

**Было:**
```
НМЦК (в копейках) *
Наша цена (в копейках)
Обеспечение заявки (в копейках)
Обеспечение контракта (в копейках)
```

**Стало:**
```
НМЦК (руб.) *
Наша цена (руб.)
Обеспечение заявки (руб.)
Обеспечение контракта (руб.)
```

### ✅ Placeholder'ы обновлены

**Было:**
- НМЦК: `10000000` (10 млн копеек)
- Наша цена: `9500000`
- Обеспечение заявки: `50000`
- Обеспечение контракта: `100000`

**Стало:**
- НМЦК: `5645255.27` (рубли с копейками)
- Наша цена: `5000000.00`
- Обеспечение заявки: `500.00`
- Обеспечение контракта: `1000.00`

### ✅ Подсказка обновлена

**Было:**
```
💡 Вводите сумму в копейках (1000000 = 10 000₽)
```

**Стало:**
```
💡 Вводите сумму в рублях (например: 5645255.27)
```

### ✅ Валидация (`validation.ts`)

**Было:**
```typescript
nmck: z.number().int().positive().min(1)
our_price: z.number().int().positive()
```

**Стало:**
```typescript
nmck: z.number().positive().min(0.01)
our_price: z.number().positive()
```

Убрали `.int()` чтобы разрешить числа с плавающей точкой (рубли с копейками).

## Как это работает

### Пользователь вводит
```
НМЦК: 5645255.27 руб
```

### Форма конвертирует перед отправкой
```typescript
nmck: Math.round(5645255.27 * 100) = 564525527 копеек
```

### В БД сохраняется
```sql
nmck: 564525527 (BIGINT, копейки)
```

### При загрузке конвертируется обратно
```typescript
nmck: 564525527 / 100 = 5645255.27 руб
```

## Преимущества

1. **Интуитивно понятно** - пользователь работает с привычными рублями
2. **Точность** - в БД хранятся копейки (без ошибок округления)
3. **Соответствие стандартам** - формат как в ЕИС
4. **Прозрачность** - автоматическая конвертация

## Тестирование

### Проверьте:
1. ✅ Создание тендера с автозаполнением из ЕИС
2. ✅ Ручное создание тендера с вводом сумм в рублях
3. ✅ Редактирование существующего тендера
4. ✅ Валидация (должны приниматься числа с точкой)
5. ✅ Сохранение в БД (должны быть копейки)

### Примеры для тестирования:
```
Ввод: 5645255.27
БД: 564525527

Ввод: 1000000.50
БД: 100000050

Ввод: 999.99
БД: 99999
```

## Связанные файлы

- `components/tenders/tender-form-modal.tsx` - UI форма
- `lib/tenders/validation.ts` - валидация
- `lib/tenders/eis-mock-data.ts` - мок-данные в рублях
- `docs/CURRENCY_CONVERSION_NOTE.md` - полная документация
