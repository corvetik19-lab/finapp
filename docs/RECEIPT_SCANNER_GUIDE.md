# 📄 Руководство по добавлению чеков

## 🎯 Возможности

Система автоматической обработки чеков позволяет:

- ✅ **Парсинг чека**: AI извлекает магазин, дату, товары, цены
- ✅ **Умное сопоставление**: Находит товары в БД по смыслу (embeddings)
- ✅ **Категории**: Каждый товар имеет категорию, которая наследуется в транзакцию
- ✅ **Автоматизация**: Создаёт транзакцию с позициями одним кликом

## 🚀 Как использовать

### 1. Откройте раздел "Финансы"

Перейдите на любую страницу в разделе финансов:
- `/finance/transactions`
- `/finance/accounts`
- `/finance/budget`
- и т.д.

### 2. Нажмите на плавающую кнопку

В правом нижнем углу вы увидите кнопку **"+"** с иконкой чека.

### 3. Вставьте текст чека

Скопируйте весь текст чека из приложения банка/магазина и вставьте в чат.

Например:
```
Поступил кассовый чек: от ООО "ПРЕМИУМ" 
Дата:  07.11.2025 19:06

1. Онигири Фуджи с Креветкой 120г
89.99 x 2.0 = 179.98 руб.

2. Батончик Корнлайн кокос 30г
16.99 x 1.0 = 16.99 руб.

Итого:  236.96 руб.
```

### 4. Дождитесь обработки

AI автоматически:
1. Распарсит чек (магазин, дата, товары, суммы)
2. Найдёт товары в вашем справочнике
3. Создаст транзакцию с позициями
4. Обновит баланс счёта

### 5. Проверьте результат

После обработки страница автоматически обновится и вы увидите новую транзакцию.

## 📦 Настройка товаров

### Перед использованием:

1. Откройте `Настройки` → `Товары`
2. Добавьте товары которые вы часто покупаете:
   - **Название**: короткое (например: "Онигири", "Молоко")
   - **Единица**: шт, кг, л и т.д.
   - **Цена**: средняя цена (опционально)
   - **Категория**: обязательно выберите категорию!

### Важно:
- ✅ Каждый товар должен иметь категорию
- ✅ Используйте короткие названия (AI сопоставит их с длинными из чека)
- ✅ Товары должны быть активны (is_active = true)

## 🤖 Как работает AI-сопоставление

### 3 уровня поиска:

1. **Точное совпадение**: "Онигири" = "Онигири" ✅
2. **Частичное совпадение**: "Онигири Фуджи" содержит "Онигири" ✅
3. **Семантическое (AI)**:
   - "Онигири Фуджи с Креветкой 120г" → "Онигири" (95% уверенности) ✅
   - "Батончик Корнлайн кокос 30г" → "Батончик" (90%) ✅
   - "Молоко Простоквашино 3.2% 1л" → "Молоко" (92%) ✅

AI понимает смысл и игнорирует:
- Бренды (Фуджи, Корнлайн, Простоквашино)
- Вес/объём (120г, 30г, 1л)
- Варианты (с Креветкой, кокос, 3.2%)

## 📊 Структура данных

### Таблицы:

```
product_items (справочник товаров)
├── id
├── user_id
├── name (короткое название)
├── category_id (ВАЖНО!)
├── default_unit
├── default_price_per_unit
└── is_active

transaction_items (позиции в транзакции)
├── id
├── transaction_id
├── user_id
├── name (из product_items)
├── category_id (из product_items)
├── quantity
├── unit
├── price_per_unit
└── total_amount
```

### Связи:
- `product_items.category_id` → `categories.id`
- `transaction_items.category_id` → `categories.id` (наследуется)

## 🔧 Применение миграции

Если вы обновились до новой версии, примените миграцию:

```bash
npx tsx scripts/apply-category-migration.ts
```

Или вручную выполните SQL из:
```
db/migrations/20251109_add_category_to_items.sql
```

## ❓ Частые вопросы

### Почему товар не найден?

1. Проверьте что товар добавлен в `Настройки` → `Товары`
2. Убедитесь что товар активен (is_active = true)
3. Проверьте что у товара есть категория
4. Используйте короткие ключевые слова (не "Онигири Фуджи", а просто "Онигири")

### Можно ли редактировать после создания?

Да! Откройте транзакцию и отредактируйте позиции вручную если нужно.

### Какой счёт используется?

По умолчанию - первый активный счёт пользователя. В будущем можно будет выбирать.

## 🎨 Кастомизация

### Изменить стили кнопки:
```css
/* components/receipts/FloatingReceiptButton.module.css */
.floatingButton {
  /* ваши стили */
}
```

### Изменить цвет градиента:
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

## 🚀 Roadmap

- [ ] Выбор счёта при добавлении чека
- [ ] История обработанных чеков
- [ ] Загрузка фото чека (OCR)
- [ ] Мульти-валютные чеки
- [ ] Экспорт в Excel/CSV

---

**Вопросы?** Напишите в техподдержку или создайте issue на GitHub.
