# 👥 Модуль управления сотрудниками

## ✅ Что реализовано

### 1. База данных
- ✅ Таблица `employees` с полным профилем сотрудника
- ✅ Таблица `employee_history` для истории изменений
- ✅ Таблица `employee_skills` для навыков и компетенций
- ✅ RLS политики для безопасности
- ✅ Триггеры для автоматического логирования
- ✅ Функция статистики `get_employees_stats()`

### 2. Роли сотрудников
- **admin** - Администратор (полный доступ)
- **manager** - Менеджер (управление тендерами)
- **tender_specialist** - Тендерный специалист
- **accountant** - Бухгалтер
- **logistics** - Логист
- **viewer** - Наблюдатель (только просмотр)

### 3. Статусы сотрудников
- **active** - Активный
- **inactive** - Неактивный
- **vacation** - В отпуске
- **dismissed** - Уволен

### 4. API Endpoints

#### GET /api/employees
Получить список сотрудников с фильтрацией

**Параметры:**
- `company_id` (обязательный)
- `search` - поиск по имени, email, телефону
- `role` - фильтр по роли
- `status` - фильтр по статусу
- `department` - фильтр по отделу
- `position` - фильтр по должности
- `page` - номер страницы (по умолчанию 1)
- `limit` - количество на странице (по умолчанию 50)
- `stats=true` - получить статистику

#### POST /api/employees
Создать нового сотрудника

**Body:**
```json
{
  "company_id": "uuid",
  "full_name": "Иван Иванов",
  "email": "ivan@example.com",
  "phone": "+79001234567",
  "role": "manager",
  "position": "Менеджер по тендерам",
  "department": "Тендерный отдел",
  "create_user_account": true,
  "password": "secure_password"
}
```

#### GET /api/employees/[id]
Получить сотрудника по ID

#### PATCH /api/employees/[id]
Обновить данные сотрудника

#### DELETE /api/employees/[id]
Удалить сотрудника (мягкое удаление)

### 5. Страница сотрудников

**URL:** `http://localhost:3000/tenders/employees`

**Функционал:**
- ✅ Список сотрудников в виде таблицы
- ✅ Фильтрация по имени, роли, статусу
- ✅ Отображение аватара, контактов, должности
- ✅ Цветовая индикация статусов
- ✅ Адаптивный дизайн

## 📊 Структура данных

### Поля сотрудника

**Персональные данные:**
- `full_name` - ФИО
- `email` - Email
- `phone` - Телефон
- `telegram` - Telegram
- `birth_date` - Дата рождения
- `avatar_url` - URL аватара

**Рабочие данные:**
- `position` - Должность
- `department` - Отдел
- `employee_number` - Табельный номер
- `hire_date` - Дата приема
- `dismissal_date` - Дата увольнения

**Роли и доступ:**
- `role` - Роль в системе
- `status` - Текущий статус
- `permissions` - Дополнительные права (JSON)

**Контакты:**
- `address` - Адрес
- `emergency_contact` - Контакт для экстренной связи
- `emergency_phone` - Телефон для экстренной связи

**Рабочие параметры:**
- `salary_amount` - Зарплата (в копейках)
- `work_schedule` - График работы
- `notes` - Заметки

## 🔐 Безопасность (RLS)

### Политики доступа

**SELECT:**
- Пользователь видит только сотрудников своей компании

**INSERT:**
- Только админы и менеджеры могут создавать сотрудников

**UPDATE:**
- Только админы и менеджеры могут обновлять данные

**DELETE:**
- Только админы могут удалять сотрудников

## 🚀 Использование

### 1. Применить миграцию БД

```bash
# В Supabase SQL Editor выполните:
db/migrations/0103_create_employees_system.sql
```

### 2. Создать первого сотрудника

```typescript
import { createEmployee } from '@/lib/employees/service';

const employee = await createEmployee({
  company_id: 'your-company-id',
  full_name: 'Администратор Системы',
  email: 'admin@company.com',
  role: 'admin',
  status: 'active',
  create_user_account: true,
  password: 'secure_password_123',
});
```

### 3. Получить список сотрудников

```typescript
import { getEmployees } from '@/lib/employees/service';

const { data, count } = await getEmployees(
  'company-id',
  { role: 'manager', status: 'active' },
  1,
  50
);
```

### 4. Получить статистику

```typescript
import { getEmployeesStats } from '@/lib/employees/service';

const stats = await getEmployeesStats('company-id');
// {
//   total_count: 15,
//   active_count: 12,
//   inactive_count: 2,
//   on_vacation_count: 1,
//   by_role: { admin: 2, manager: 5, ... }
// }
```

## 📝 TODO (Будущие улучшения)

### Модалка добавления/редактирования
- [ ] Форма с валидацией (React Hook Form + Zod)
- [ ] Загрузка аватара
- [ ] Выбор роли с описанием прав
- [ ] Создание учетной записи пользователя
- [ ] Назначение на тендеры

### Страница профиля сотрудника
- [ ] Детальная информация
- [ ] История изменений
- [ ] Список навыков
- [ ] Назначенные тендеры
- [ ] Статистика эффективности

### Дополнительный функционал
- [ ] Импорт сотрудников из CSV/Excel
- [ ] Экспорт списка сотрудников
- [ ] Массовые операции (изменение статуса, роли)
- [ ] Уведомления о днях рождения
- [ ] Календарь отпусков
- [ ] Табель учета рабочего времени

### Интеграция с тендерами
- [ ] Автоматическое назначение менеджера на тендер
- [ ] Фильтр тендеров по ответственному
- [ ] Статистика по сотрудникам (выигранные тендеры, сумма контрактов)
- [ ] Рейтинг эффективности

## 🔗 Связанные файлы

**База данных:**
- `db/migrations/0103_create_employees_system.sql`

**Backend:**
- `lib/employees/types.ts` - Типы и интерфейсы
- `lib/employees/validation.ts` - Схемы валидации
- `lib/employees/service.ts` - Сервисный слой
- `app/api/employees/route.ts` - API endpoints
- `app/api/employees/[id]/route.ts` - API для конкретного сотрудника

**Frontend:**
- `app/(protected)/tenders/employees/page.tsx` - Страница
- `app/(protected)/tenders/employees/employees-list-client.tsx` - Клиентский компонент

## 💡 Примеры

### Создание сотрудника с учетной записью

```typescript
const newEmployee = await createEmployee({
  company_id: companyId,
  full_name: 'Петр Петров',
  email: 'petr@company.com',
  phone: '+79001234567',
  telegram: '@petr_petrov',
  role: 'tender_specialist',
  position: 'Тендерный специалист',
  department: 'Тендерный отдел',
  hire_date: '2025-01-15',
  create_user_account: true,
  password: 'SecurePass123!',
});
```

### Обновление роли сотрудника

```typescript
await updateEmployee(employeeId, {
  role: 'manager',
  position: 'Старший менеджер',
});
```

### Поиск сотрудников

```typescript
const { data } = await getEmployees(companyId, {
  search: 'Иван',
  status: 'active',
  department: 'Тендерный отдел',
});
```

## 🎨 UI Компоненты

### Таблица сотрудников
- Аватар с инициалами
- ФИО и табельный номер
- Email и телефон
- Должность и отдел
- Роль (badge)
- Статус (цветной badge)
- Кнопка редактирования

### Фильтры
- Поиск по тексту
- Выбор роли
- Выбор статуса

### Статусы (цвета)
- Активный - зеленый (#10b981)
- Неактивный - серый (#6b7280)
- В отпуске - синий (#3b82f6)
- Уволен - красный (#ef4444)

---

**Модуль готов к использованию!** 🎉

Для полного функционала необходимо:
1. Применить миграцию БД
2. Создать модалку добавления/редактирования
3. Добавить страницу профиля сотрудника
