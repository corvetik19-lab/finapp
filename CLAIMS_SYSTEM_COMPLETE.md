# Система взыскания долгов - Завершено ✅

## Обзор

Система взыскания долгов в режиме "Тендеры" полностью переделана по образцу CRM системы (https://ip-domasnix.ztcs.biz/claims).

## Что реализовано

### 1. База данных

**Миграция:** `db/migrations/20251119_extend_debts_for_claims.sql`

Добавлены новые поля в таблицу `debts`:
- `tender_id` - связь с тендером (UUID, nullable)
- `application_number` - номер заявки (TEXT)
- `contract_number` - номер договора (TEXT)
- `stage` - этап взыскания (ENUM: new, claim, court, writ, bailiff, paid)
- `plaintiff` - истец/наша организация (TEXT)
- `defendant` - ответчик/должник (TEXT)
- `comments` - комментарии по претензии (TEXT)

### 2. Типы и валидация

**Файлы:**
- `types/debt.ts` - расширенный интерфейс `Debt` с новыми полями
- `lib/validation/debt.ts` - обновлённая схема валидации `debtFormSchema`

**Новые типы:**
```typescript
export type ClaimStage = 
  | 'new'        // Новые
  | 'claim'      // Претензия отправлена
  | 'court'      // Реестр заседаний (суд)
  | 'writ'       // Исполнительный лист получен
  | 'bailiff'    // Отправлено приставам
  | 'paid';      // Оплачено

export const CLAIM_STAGE_LABELS: Record<ClaimStage, string>
```

### 3. Сервисный слой

**Файл:** `lib/debts/service.ts`

Обновлённые функции:
- `getDebts()` - получение всех претензий
- `createDebt()` - создание с новыми полями
- `updateDebt()` - обновление с поддержкой новых полей
- `deleteDebt()` - мягкое удаление
- `getTenders()` - получение списка тендеров для автокомплита

### 4. UI компоненты

#### ClaimsTable (Таблица претензий)
**Файл:** `components/debts/ClaimsTable.tsx`

**Функционал:**
- ✅ Табличное отображение претензий (как в CRM)
- ✅ Фильтрация по этапам (табы сверху)
- ✅ Поиск по всем текстовым полям
- ✅ Изменение этапа прямо в таблице (select)
- ✅ Действия: просмотр, удаление
- ✅ Пагинация (UI готов)
- ✅ Адаптивный дизайн

**Колонки таблицы:**
1. ID (порядковый номер)
2. № Тендера
3. № Заявки
4. Истец
5. Ответчик
6. Основной долг (сумма)
7. № Договора
8. Этап (с цветовой индикацией)
9. Комментарии
10. Действия

**Цветовая индикация этапов:**
- Новые - серый
- Претензия - оранжевый
- Суд - красный
- Исполнительный лист - фиолетовый
- Приставы - синий
- Оплачено - зелёный

#### ClaimFormModal (Форма претензии)
**Файл:** `components/debts/ClaimFormModal.tsx`

**Поля формы:**
- Тип (Я должен / Мне должны)
- Тендер (выбор из списка, опционально)
- № Заявки
- № Договора
- Истец
- Ответчик
- Имя кредитора/должника
- Основной долг + валюта
- Дата возникновения
- Срок возврата
- Этап взыскания
- Описание
- Комментарии

#### ClaimsManager (Менеджер претензий)
**Файл:** `components/debts/ClaimsManager.tsx`

Объединяет:
- Заголовок страницы
- Кнопку "ДОБАВИТЬ ПРЕТЕНЗИЮ" (синяя, как в CRM)
- Таблицу претензий
- Модальную форму

### 5. Страница

**Файл:** `app/(protected)/tenders/claims/page.tsx`

Server Component, который:
- Загружает данные через `getDebts()`
- Рендерит `ClaimsManager`
- Использует `force-dynamic` для актуальности данных

### 6. Стили

**Файлы:**
- `components/debts/ClaimsTable.module.css` - стили таблицы
- `components/debts/ClaimsManager.module.css` - стили менеджера
- `components/debts/DebtFormModal.module.css` - переиспользуется для форм

**Особенности:**
- Современный дизайн в стиле вашего приложения
- Плавные переходы и анимации
- Адаптивность для мобильных устройств
- Цветовая индикация статусов

## Как использовать

### 1. Применить миграцию БД

```bash
# Через Supabase Dashboard (SQL Editor)
# Скопируйте содержимое db/migrations/20251119_extend_debts_for_claims.sql
# и выполните в SQL Editor
```

Подробная инструкция: `APPLY_CLAIMS_MIGRATION.md`

### 2. Запустить приложение

```bash
npm run dev
```

### 3. Открыть страницу

Перейдите в режим "Тендеры" → "Взыскание долгов"

URL: `/tenders/claims`

## Функционал

### Добавление претензии
1. Нажмите "ДОБАВИТЬ ПРЕТЕНЗИЮ"
2. Заполните форму (обязательные поля: имя, сумма, дата)
3. Опционально: выберите тендер, укажите номера заявки/договора
4. Сохраните

### Фильтрация
- Используйте табы сверху для фильтрации по этапам
- Или выберите "Все записи о претензиях"
- Счётчики показывают количество претензий на каждом этапе

### Поиск
- Введите текст в поле поиска
- Поиск работает по всем текстовым полям

### Изменение этапа
- Выберите новый этап в выпадающем списке прямо в таблице
- Изменения сохраняются автоматически

### Удаление
- Нажмите иконку корзины
- Подтвердите удаление

## Отличия от CRM

### Сохранено из CRM:
✅ Табличное отображение
✅ Фильтрация по этапам (табы)
✅ Поиск
✅ Колонки: ID, № Тендера, № Заявки, Истец, Ответчик, Долг, № Договора, Комментарии
✅ Кнопка "ДОБАВИТЬ ПРЕТЕНЗИЮ"
✅ Пагинация (UI)

### Адаптировано под ваше приложение:
🎨 Стили в едином стиле с остальным приложением
🎨 Material Icons вместо других иконок
🎨 Цветовая схема вашего приложения
🔗 Интеграция с тендерами через `tender_id`
🔗 Использование существующей таблицы `debts`
📱 Адаптивный дизайн

## Файловая структура

```
app/(protected)/tenders/claims/
  └── page.tsx                          # Главная страница

components/debts/
  ├── ClaimsManager.tsx                 # Менеджер претензий
  ├── ClaimsManager.module.css
  ├── ClaimsTable.tsx                   # Таблица претензий
  ├── ClaimsTable.module.css
  ├── ClaimFormModal.tsx                # Форма претензии
  ├── DebtFormModal.tsx                 # Старая форма (сохранена)
  └── DebtFormModal.module.css

lib/debts/
  └── service.ts                        # Сервисные функции

types/
  └── debt.ts                           # Типы

db/migrations/
  └── 20251119_extend_debts_for_claims.sql  # Миграция БД
```

## Следующие шаги (опционально)

### Возможные улучшения:
1. **Детальная страница претензии** (`/tenders/claims/[id]`)
   - История изменений
   - Прикреплённые документы
   - Комментарии/переписка

2. **Реальная пагинация**
   - Server-side пагинация
   - Параметры в URL

3. **Экспорт в Excel/PDF**
   - Экспорт таблицы
   - Печать претензии

4. **Уведомления**
   - О приближении сроков
   - О смене этапа

5. **Статистика**
   - Дашборд с графиками
   - Аналитика по этапам

6. **Интеграция с документами**
   - Генерация претензий
   - Шаблоны документов

## Проверка работы

1. ✅ TypeScript компилируется без ошибок
2. ✅ Все компоненты созданы
3. ✅ Стили применены
4. ✅ Миграция БД готова
5. ✅ Сервисные функции обновлены

**Статус:** Готово к использованию после применения миграции БД! 🎉
